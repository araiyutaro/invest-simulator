---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: [01-foundation/01-02]
files_modified:
  - lib/session.ts
  - lib/auth.ts
  - app/login/page.tsx
  - app/api/auth/login/route.ts
  - app/api/auth/logout/route.ts
autonomous: true
requirements: [SEC-01, SEC-03]
must_haves:
  truths:
    - "/login ページでパスワード単一入力フォームが表示される（D-12）"
    - "正しいパスワードを POST すると iron-session が isAuthenticated=true で暗号化 cookie を発行する"
    - "誤ったパスワードは 401 + 「パスワードが違います」メッセージを返す（D-15）"
    - "セッション cookie は httpOnly, secure(prod), maxAge=60*60*24*30（D-13）"
    - "パスワード比較は timingSafeEqual で定数時間比較される（D-16）"
  artifacts:
    - path: "lib/session.ts"
      provides: "sessionOptions + SessionData type"
      contains: "import 'server-only'"
    - path: "lib/auth.ts"
      provides: "verifyPassword with timingSafeEqual"
      contains: "timingSafeEqual"
    - path: "app/login/page.tsx"
      provides: "login form (single password input)"
      contains: "<form"
    - path: "app/api/auth/login/route.ts"
      provides: "POST login handler"
      contains: "getIronSession"
  key_links:
    - from: "app/api/auth/login/route.ts"
      to: "lib/session.ts"
      via: "getIronSession(await cookies(), sessionOptions)"
      pattern: "getIronSession"
    - from: "lib/auth.ts"
      to: "lib/env.ts"
      via: "env.SITE_PASSWORD"
      pattern: "env\\.SITE_PASSWORD"
---

<objective>
iron-session v8 ベースのパスワード保護を構築する。`/login` ページ、`POST /api/auth/login`、`POST /api/auth/logout` を実装し、成功時に 30 日有効の暗号化セッション cookie を発行する。

Purpose: Plan 04 の proxy.ts gate が読み取る cookie を発行する側。SEC-01 (パスワード保護) と SEC-03 (セッション情報がクライアントに漏れない) を達成する。
Output: lib/session.ts, lib/auth.ts, app/login/page.tsx, app/api/auth/login/route.ts, app/api/auth/logout/route.ts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@lib/env.ts
@./AGENTS.md

<interfaces>
iron-session v8 App Router API — from RESEARCH.md Pattern 4:

```typescript
import { cookies } from 'next/headers'
import { getIronSession } from 'iron-session'

// In Route Handler / Server Action ONLY (NOT in proxy.ts):
const session = await getIronSession<SessionData>(await cookies(), sessionOptions)
session.isAuthenticated = true
await session.save()
```

Note: `cookies()` returns `Promise<ReadonlyRequestCookies>` in Next.js 15+/16, so it must be awaited before passing to `getIronSession`.

lib/env.ts export (from Plan 02):
```typescript
export const env: {
  DATABASE_URL: string
  ANTHROPIC_API_KEY: string
  SESSION_SECRET: string
  SITE_PASSWORD: string
  CRON_SECRET: string
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install iron-session + define session/auth libs</name>
  <files>package.json, lib/session.ts, lib/auth.ts</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md Pattern 4, Pattern 5, Pitfall 5
    - .planning/phases/01-foundation/01-CONTEXT.md D-13, D-14, D-16
    - lib/env.ts（Plan 02 成果物、env export の型を確認）
  </read_first>
  <action>
    **Step 1**: `npm install iron-session@^8.0.4` を実行する。

    **Step 2**: `lib/session.ts` を作成:

    ```typescript
    import 'server-only'
    import { env } from './env'

    export type SessionData = {
      isAuthenticated: boolean
    }

    export const sessionOptions = {
      password: env.SESSION_SECRET,
      cookieName: 'invest-sim-session',
      cookieOptions: {
        httpOnly: true,
        secure: process.env.NODE_ENV === 'production',
        maxAge: 60 * 60 * 24 * 30, // D-13: 30 days
        sameSite: 'lax' as const,
        path: '/',
      },
    }

    export const defaultSession: SessionData = {
      isAuthenticated: false,
    }
    ```

    **Step 3**: `lib/auth.ts` を作成（D-16 + RESEARCH.md Pitfall 5 対策：長さ不一致 early return）:

    ```typescript
    import 'server-only'
    import { timingSafeEqual } from 'crypto'
    import { env } from './env'

    export function verifyPassword(input: string): boolean {
      const inputBuf = Buffer.from(input, 'utf8')
      const passwordBuf = Buffer.from(env.SITE_PASSWORD, 'utf8')

      // timingSafeEqual throws on length mismatch. Password length is not secret.
      if (inputBuf.length !== passwordBuf.length) return false

      return timingSafeEqual(inputBuf, passwordBuf)
    }
    ```

    NEVER: bcrypt や PBKDF2 を使う（D-16 で平文+timingSafeEqual が決定済み）。レートリミットは無し（D-15、自分専用）。
  </action>
  <verify>
    <automated>test -f lib/session.ts &amp;&amp; test -f lib/auth.ts &amp;&amp; head -1 lib/session.ts | grep -q "server-only" &amp;&amp; head -1 lib/auth.ts | grep -q "server-only" &amp;&amp; grep -q "maxAge: 60 \* 60 \* 24 \* 30" lib/session.ts &amp;&amp; grep -q "timingSafeEqual" lib/auth.ts &amp;&amp; grep -q "inputBuf.length !== passwordBuf.length" lib/auth.ts &amp;&amp; node -e "const p=require('./package.json');if(!p.dependencies['iron-session'])process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - package.json に iron-session dependency が存在
    - lib/session.ts 1行目が `import 'server-only'`
    - lib/session.ts が maxAge を 60*60*24*30 で設定
    - lib/session.ts が cookieName を 'invest-sim-session' で設定
    - lib/auth.ts が timingSafeEqual + length pre-check を実装
    - lib/auth.ts が env.SITE_PASSWORD を参照（process.env 直参照不可）
  </acceptance_criteria>
  <done>lib/session.ts と lib/auth.ts がコミット可能</done>
</task>

<task type="auto">
  <name>Task 2: Login page + /api/auth/login + /api/auth/logout</name>
  <files>app/login/page.tsx, app/api/auth/login/route.ts, app/api/auth/logout/route.ts</files>
  <read_first>
    - node_modules/next/dist/docs/01-app/03-api-reference/04-functions/cookies.md（Next.js 16 の cookies() API）
    - .planning/phases/01-foundation/01-RESEARCH.md Pattern 4
    - .planning/phases/01-foundation/01-CONTEXT.md D-12, D-15
  </read_first>
  <action>
    **Step 1**: `app/login/page.tsx` を Client Component として作成（form の `onSubmit` が必要）。D-12: パスワード単一入力のみ、ユーザー名入力なし。Tailwind でシンプルに:

    ```typescript
    'use client'

    import { useState } from 'react'
    import { useRouter } from 'next/navigation'

    export default function LoginPage() {
      const [password, setPassword] = useState('')
      const [error, setError] = useState<string | null>(null)
      const [submitting, setSubmitting] = useState(false)
      const router = useRouter()

      async function handleSubmit(e: React.FormEvent) {
        e.preventDefault()
        setSubmitting(true)
        setError(null)
        const res = await fetch('/api/auth/login', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ password }),
        })
        setSubmitting(false)
        if (res.ok) {
          router.push('/dashboard')
          router.refresh()
        } else {
          setError('パスワードが違います')  // D-15 exact wording
        }
      }

      return (
        <main className="min-h-screen flex items-center justify-center bg-gray-50">
          <form onSubmit={handleSubmit} className="w-full max-w-sm bg-white p-8 rounded-lg shadow">
            <h1 className="text-xl font-semibold mb-6">invest-simulator</h1>
            <label className="block text-sm font-medium mb-2" htmlFor="password">Password</label>
            <input
              id="password"
              type="password"
              value={password}
              onChange={(e) => setPassword(e.target.value)}
              className="w-full px-3 py-2 border rounded mb-4"
              required
              autoFocus
            />
            {error && <p className="text-sm text-red-600 mb-4">{error}</p>}
            <button
              type="submit"
              disabled={submitting}
              className="w-full bg-black text-white py-2 rounded disabled:opacity-50"
            >
              {submitting ? 'Signing in…' : 'Sign in'}
            </button>
          </form>
        </main>
      )
    }
    ```

    **Step 2**: `app/api/auth/login/route.ts` を作成:

    ```typescript
    import { cookies } from 'next/headers'
    import { getIronSession } from 'iron-session'
    import { sessionOptions, type SessionData } from '@/lib/session'
    import { verifyPassword } from '@/lib/auth'

    export async function POST(request: Request) {
      let body: { password?: unknown }
      try {
        body = await request.json()
      } catch {
        return Response.json({ error: 'Invalid JSON' }, { status: 400 })
      }

      const password = typeof body.password === 'string' ? body.password : ''

      if (!verifyPassword(password)) {
        // D-15: 401 + 「パスワードが違います」
        return Response.json({ error: 'パスワードが違います' }, { status: 401 })
      }

      const session = await getIronSession<SessionData>(await cookies(), sessionOptions)
      session.isAuthenticated = true
      await session.save()

      return Response.json({ success: true })
    }
    ```

    **Step 3**: `app/api/auth/logout/route.ts` を作成:

    ```typescript
    import { cookies } from 'next/headers'
    import { getIronSession } from 'iron-session'
    import { sessionOptions, type SessionData } from '@/lib/session'

    export async function POST() {
      const session = await getIronSession<SessionData>(await cookies(), sessionOptions)
      session.destroy()
      return Response.json({ success: true })
    }
    ```

    **Next.js 16 注意**: `cookies()` は `Promise<ReadonlyRequestCookies>` を返すので必ず `await cookies()` で使う。古い同期 API（`cookies().get(...)`）は Next.js 16 で動作しない。必要なら実装前に `node_modules/next/dist/docs/01-app/03-api-reference/04-functions/cookies.md` を確認する。

    NEVER: proxy.ts 内で cookies() を使う（Pitfall 2 — そちらは request.cookies as any を使う。Plan 04 で対応）。NEVER: この Route Handler を `export const runtime = 'edge'` にする（iron-session は Node.js runtime 前提）。
  </action>
  <verify>
    <automated>test -f app/login/page.tsx &amp;&amp; test -f app/api/auth/login/route.ts &amp;&amp; test -f app/api/auth/logout/route.ts &amp;&amp; grep -q "'use client'" app/login/page.tsx &amp;&amp; grep -q 'type="password"' app/login/page.tsx &amp;&amp; grep -q "パスワードが違います" app/login/page.tsx &amp;&amp; grep -q "パスワードが違います" app/api/auth/login/route.ts &amp;&amp; grep -q "getIronSession" app/api/auth/login/route.ts &amp;&amp; grep -q "await cookies()" app/api/auth/login/route.ts &amp;&amp; grep -q "session.destroy" app/api/auth/logout/route.ts &amp;&amp; npx tsc --noEmit --skipLibCheck</automated>
  </verify>
  <acceptance_criteria>
    - app/login/page.tsx が `'use client'` directive を持つ
    - login page form に `<input type="password" ...>` が1つだけ存在（ユーザー名欄なし — D-12）
    - login page のエラー文言は「パスワードが違います」
    - POST /api/auth/login が 401 応答時に `{ error: 'パスワードが違います' }` を返す
    - POST /api/auth/login が `getIronSession<SessionData>(await cookies(), sessionOptions)` を使う
    - POST /api/auth/logout が `session.destroy()` を呼ぶ
    - `npx tsc --noEmit --skipLibCheck` が 0 エラー
  </acceptance_criteria>
  <done>Login flow の server+client が全て書かれ、型チェックが通る</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client browser → /api/auth/login | untrusted password input |
| session cookie → server | signed/encrypted, cannot be forged without SESSION_SECRET |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | S (Spoofing) | /api/auth/login | mitigate | timingSafeEqual パスワード比較、iron-session の AEAD 暗号化 cookie |
| T-01-10 | T (Tampering) | session cookie | mitigate | iron-session が HMAC 付き暗号化、改ざんは検出される |
| T-01-11 | I | login response | mitigate | パスワード自体は response body に含めない。エラーは固定文言のみ |
| T-01-12 | D (DoS) | login endpoint | accept | レートリミットなし（D-15、自分専用のため） |
| T-01-13 | S | session hijack via XSS | mitigate | cookie は httpOnly, sameSite:'lax', prod は secure。JS から読めない |
</threat_model>

<verification>
- `grep -c 'type="password"' app/login/page.tsx` == 1（単一入力）
- `grep "getIronSession" app/api/auth/login/route.ts` matches
- `npx tsc --noEmit --skipLibCheck` で 0 エラー
- 401 error message は正確に「パスワードが違います」
</verification>

<success_criteria>
Phase 1 Success Criteria #1（正しいパスワードでダッシュボードアクセス）と #2（誤ったパスワードで 401）に必要な「発行側」が完成。検証は Plan 04（proxy gate 追加後）の human-verify で行う。
</success_criteria>

<output>
.planning/phases/01-foundation/01-03-SUMMARY.md に結果を記録
</output>
