---
phase: 01-foundation
plan: 04
type: execute
wave: 3
depends_on: [01-foundation/01-03]
files_modified:
  - proxy.ts
  - app/dashboard/page.tsx
  - app/page.tsx
autonomous: false
requirements: [SEC-01]
must_haves:
  truths:
    - "未認証で /dashboard にアクセスすると /login にリダイレクトされる（D-14）"
    - "認証済み cookie で /dashboard にアクセスするとページが表示される"
    - "/login と /api/cron/* は proxy の認証チェックから除外される（D-14）"
    - "curl で /dashboard にアクセスしても trade データ等は一切返されない（SEC-01, PITFALLS #9）"
    - "ブラウザを閉じて再度開いても、30日以内なら /dashboard に再アクセスできる"
  artifacts:
    - path: "proxy.ts"
      provides: "Next.js 16 auth gate (proxy function, NOT middleware)"
      contains: "export async function proxy"
    - path: "app/dashboard/page.tsx"
      provides: "Protected dashboard placeholder"
      contains: "dashboard"
  key_links:
    - from: "proxy.ts"
      to: "lib/session.ts"
      via: "getIronSession(request.cookies as any, sessionOptions)"
      pattern: "getIronSession"
    - from: "proxy.ts"
      to: "/login redirect"
      via: "NextResponse.redirect"
      pattern: "NextResponse\\.redirect"
---

<objective>
Next.js 16 の `proxy.ts`（旧 middleware.ts）で認証ガードを実装する。未認証リクエストを `/login` にリダイレクトし、`/login` と `/api/cron/*` のみ除外する（D-14）。併せて保護対象の `/dashboard` ページ placeholder を作る。

Purpose: Plan 03 で発行された cookie を proxy.ts で読み取り、Phase 1 Success Criteria #1 と #2 を完成させる。Next.js 16 の `proxy.ts` ファイル名とシンボル名（`proxy`、not `middleware`）を守ることが critical（AGENTS.md の警告）。
Output: proxy.ts, app/dashboard/page.tsx, app/page.tsx（root redirect）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@lib/session.ts
@./AGENTS.md

<interfaces>
Next.js 16 proxy.ts API — from RESEARCH.md Pattern 4 + 01-RESEARCH.md Pitfall 2:

```typescript
// proxy.ts at project root (NOT middleware.ts — Next.js 16 renamed it)
import { NextResponse, type NextRequest } from 'next/server'

export async function proxy(request: NextRequest) {
  // ...
  return NextResponse.next()
}

export const config = { matcher: [...] }
```

CRITICAL: Inside proxy, use `request.cookies as any` (NOT `await cookies()` from 'next/headers' — that fails due to missing async storage context; Pitfall 2).

From lib/session.ts (Plan 03):
```typescript
export const sessionOptions: { password, cookieName, cookieOptions }
export type SessionData = { isAuthenticated: boolean }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create proxy.ts (Next.js 16 auth gate) + dashboard placeholder + root redirect</name>
  <files>proxy.ts, app/dashboard/page.tsx, app/page.tsx</files>
  <read_first>
    - ./AGENTS.md (Next.js 16 は破壊的変更あり — proxy.ts を使う)
    - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/proxy.md (もし存在すれば Next.js 16 proxy API 仕様)
    - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/middleware.md (deprecation note)
    - .planning/phases/01-foundation/01-RESEARCH.md Pattern 4 + Pitfall 1 + Pitfall 2
    - .planning/phases/01-foundation/01-CONTEXT.md D-14
  </read_first>
  <action>
    **Step 1**: プロジェクトルートに `proxy.ts` を作成する（`middleware.ts` ではない — Next.js 16 のファイル名契約）。`app/proxy.ts` でも `src/proxy.ts` でもなく、package.json と同じディレクトリに置く:

    ```typescript
    // proxy.ts — Next.js 16 auth gate (file was middleware.ts in Next.js <=15)
    // Ref: node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/proxy.md
    import { NextResponse, type NextRequest } from 'next/server'
    import { getIronSession } from 'iron-session'
    import { sessionOptions, type SessionData } from '@/lib/session'

    export async function proxy(request: NextRequest) {
      const { pathname } = request.nextUrl

      // D-14: /login と /api/cron/* のみ除外
      if (
        pathname === '/login' ||
        pathname.startsWith('/login/') ||
        pathname.startsWith('/api/auth/') ||
        pathname.startsWith('/api/cron/')
      ) {
        return NextResponse.next()
      }

      // CRITICAL (Pitfall 2): inside proxy use request.cookies, NOT cookies() from next/headers.
      // cookies() requires async storage context not present in proxy runtime.
      const session = await getIronSession<SessionData>(
        request.cookies as any,
        sessionOptions
      )

      if (!session.isAuthenticated) {
        const loginUrl = new URL('/login', request.url)
        return NextResponse.redirect(loginUrl)
      }

      return NextResponse.next()
    }

    export const config = {
      // 静的ファイル・画像最適化・favicon を除外し、それ以外全てを proxy 経由にする
      matcher: [
        '/((?!_next/static|_next/image|favicon.ico).*)',
      ],
    }
    ```

    注意: `/api/auth/*` はログインエンドポイント自身なので除外する（そうしないとログイン時にリダイレクトループになる）。

    **Step 2**: `app/dashboard/page.tsx` を Server Component として placeholder 作成:

    ```typescript
    export default function DashboardPage() {
      return (
        <main className="min-h-screen p-8">
          <h1 className="text-2xl font-semibold mb-4">Dashboard</h1>
          <p className="text-gray-600">
            Phase 1 foundation complete. Market data, agent runs, and charts arrive in Phase 2-4.
          </p>
          <form action="/api/auth/logout" method="POST" className="mt-8">
            <button type="submit" className="text-sm text-red-600 underline">
              Sign out
            </button>
          </form>
        </main>
      )
    }
    ```

    Note: プレーン HTML form は fetch せずにページ遷移するので logout 後のリダイレクト先を持たない。簡易対応として logout POST 後に `/login` にリダイレクトする必要がある場合は、後続フェーズで client-side fetch に切り替える。Phase 1 の「sign out できる」目的には十分。

    **Step 3**: `app/page.tsx` を「`/dashboard` にリダイレクト」の形で作る（root `/` アクセスを処理）:

    ```typescript
    import { redirect } from 'next/navigation'

    export default function RootPage() {
      redirect('/dashboard')
    }
    ```

    proxy.ts が `/` に対して認証チェックを行い、未認証なら `/login` に飛ばし、認証済みなら `redirect('/dashboard')` が走る。

    NEVER: `middleware.ts` というファイル名を使う（Next.js 16 で非推奨、将来削除予定）。NEVER: proxy 内で `cookies()` を使う（Pitfall 2）。NEVER: matcher から `/api/(.*)` を丸ごと除外する（dashboard API も保護する必要があるため）。
  </action>
  <verify>
    <automated>test -f proxy.ts &amp;&amp; ! test -f middleware.ts &amp;&amp; grep -q "export async function proxy" proxy.ts &amp;&amp; grep -q "request.cookies as any" proxy.ts &amp;&amp; grep -q "pathname === '/login'" proxy.ts &amp;&amp; grep -q "pathname.startsWith('/api/cron/')" proxy.ts &amp;&amp; grep -q "pathname.startsWith('/api/auth/')" proxy.ts &amp;&amp; grep -q "NextResponse.redirect" proxy.ts &amp;&amp; test -f app/dashboard/page.tsx &amp;&amp; test -f app/page.tsx &amp;&amp; grep -q "redirect('/dashboard')" app/page.tsx &amp;&amp; npx tsc --noEmit --skipLibCheck</automated>
  </verify>
  <acceptance_criteria>
    - `proxy.ts` が project root に存在する
    - `middleware.ts` は存在しない（negative assertion、Next.js 16 契約）
    - `proxy.ts` の関数名は `proxy`（`middleware` ではない）
    - `proxy.ts` が `request.cookies as any` を getIronSession に渡している
    - `proxy.ts` が `/login`, `/api/auth/*`, `/api/cron/*` を除外する
    - `proxy.ts` が `NextResponse.redirect(new URL('/login', request.url))` で未認証を返す
    - `app/dashboard/page.tsx` が存在し、logout form を含む
    - `app/page.tsx` が `/dashboard` にリダイレクトする
    - `npx tsc --noEmit --skipLibCheck` が 0 エラー
  </acceptance_criteria>
  <done>proxy.ts + dashboard + root redirect がコミット可能な状態</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [HUMAN VERIFY] End-to-end auth flow</name>
  <files></files>
  <read_first>
    - .planning/ROADMAP.md Phase 1 Success Criteria #1, #2, #4
    - .planning/research/PITFALLS.md #9 (Password protection leaks via SSR)
  </read_first>
  <what-built>
    Plan 01 (DB schema)、Plan 02 (env)、Plan 03 (login + session)、Plan 04 (proxy gate + dashboard) まで完了。
    これでエンドツーエンドの認証フローが初めて動く状態になった。
  </what-built>
  <how-to-verify>
    手動で次を検証する:

    **準備**:
    1. `.env.local` に以下全てが設定されていることを確認:
       - `DATABASE_URL=...` (Plan 01 で設定済み)
       - `ANTHROPIC_API_KEY=...` (Anthropic Console から取得)
       - `SESSION_SECRET=...` (`openssl rand -base64 48` で生成、32文字以上)
       - `SITE_PASSWORD=test-password-xyz` (自分で決めた値)
       - `CRON_SECRET=...` (任意の32文字文字列)
    2. `npm run dev` で dev サーバー起動

    **Test 1 — 未認証 curl が保護される (SEC-01, PITFALLS #9)**:
    ```bash
    curl -sv http://localhost:3000/dashboard 2>&1 | grep -E "(HTTP|location)"
    ```
    期待: `HTTP/1.1 307` (or 302) + `location: /login`。レスポンスボディに "dashboard" という文字列が含まれないこと（`curl -s http://localhost:3000/dashboard | grep -q dashboard && echo LEAK || echo OK`）。

    **Test 2 — ブラウザで未認証アクセス**:
    1. ブラウザ DevTools → Application → Cookies で全 cookie 削除
    2. `http://localhost:3000/dashboard` にアクセス
    3. 期待: `/login` にリダイレクトされ、パスワード入力フォームが表示される

    **Test 3 — 誤ったパスワード (D-15)**:
    1. `/login` で間違ったパスワードを入力して送信
    2. 期待: ページ上に「パスワードが違います」と表示、リダイレクトされない、DevTools Network タブで 401 応答を確認

    **Test 4 — 正しいパスワード (Success Criteria #1)**:
    1. `/login` で `.env.local` の `SITE_PASSWORD` 値を入力して送信
    2. 期待: `/dashboard` にリダイレクトされ、"Dashboard" 見出しが表示される
    3. DevTools → Application → Cookies → `invest-sim-session` cookie が存在、httpOnly=true を確認

    **Test 5 — セッション永続性 (Success Criteria #1 後半)**:
    1. ブラウザを完全に閉じる（または全タブ閉じる）
    2. ブラウザを再度開いて `http://localhost:3000/dashboard` にアクセス
    3. 期待: ログインフォームを経由せず、直接ダッシュボードが表示される

    **Test 6 — NEXT_PUBLIC 漏洩チェック (D-21, SEC-02)**:
    ```bash
    curl -s http://localhost:3000/login | grep -i "site_password\|session_secret\|anthropic\|database_url\|NEXT_PUBLIC_" && echo LEAK || echo OK
    ```
    期待: `OK`

    **Test 7 — API route も保護 (PITFALLS #9, curl 検証)**:
    ```bash
    curl -sv http://localhost:3000/api/nonexistent-dashboard-data 2>&1 | grep -E "(HTTP|location)"
    ```
    期待: 307 redirect to /login（matcher が API も captures する）。`/api/auth/login` と `/api/cron/*` だけが除外されることを確認:
    ```bash
    curl -sv -X POST http://localhost:3000/api/auth/login -H "Content-Type: application/json" -d '{"password":"wrong"}' 2>&1 | grep HTTP
    ```
    期待: 401（redirect されずに Route Handler まで到達している）

    全て PASS なら "approved"。1つでも失敗したらどのテストがどう失敗したかを伝える。
  </how-to-verify>
  <action>See <how-to-verify> section above. This is a checkpoint task — human executes the described steps and signals completion via the resume-signal.</action>
  <verify>
    <automated>MANUAL — verification is performed by the human per the how-to-verify steps. Claude proceeds only after 'approved' resume signal.</automated>
  </verify>
  <acceptance_criteria>
    - Test 1: curl /dashboard は 307→/login、body に "dashboard" 文字列なし
    - Test 2: ブラウザ未認証→ /login リダイレクト
    - Test 3: 誤パスワード→ 401 + 「パスワードが違います」表示
    - Test 4: 正パスワード→ /dashboard 表示 + invest-sim-session cookie 発行 (httpOnly)
    - Test 5: ブラウザ再起動後も /dashboard アクセス可能
    - Test 6: HTML ソースに secret 値・NEXT_PUBLIC_ 無し
    - Test 7: /api/* は保護される（/api/auth/login と /api/cron/* 除く）
  </acceptance_criteria>
  <resume-signal>Type "approved" once all 7 tests pass</resume-signal>
  <done>Phase 1 Success Criteria #1, #2, #4 が human-verified</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internet → proxy.ts | All requests pass through auth gate |
| proxy → Route Handler | Only if session.isAuthenticated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-14 | S (Spoofing) | proxy.ts | mitigate | iron-session seal 検証により cookie 改ざんを検出、不正 cookie は isAuthenticated=false |
| T-01-15 | I (SSR leak) | /dashboard Server Component | mitigate | proxy.ts が Server Component 実行前にリダイレクト。PITFALLS #9 対策 |
| T-01-16 | E (Elevation) | matcher config | mitigate | `/((?!_next/static|_next/image|favicon.ico).*)` で全ルートを capture（/api も含む） |
| T-01-17 | T (Tampering) | redirect loop | mitigate | /login と /api/auth/* を除外、isAuthenticated チェック失敗時のみ redirect |
| T-01-18 | S | Next.js 16 symbol name | mitigate | ファイル名 proxy.ts、関数名 proxy。middleware.ts を作らない（非推奨） |
</threat_model>

<verification>
- `test -f proxy.ts && ! test -f middleware.ts`
- Test 1–7 全てパス（human-verified）
</verification>

<success_criteria>
Phase 1 Success Criteria #1（セッション永続）、#2（誤パスワード 401）、#4（NEXT_PUBLIC_* 不在）が完全達成。
</success_criteria>

<output>
.planning/phases/01-foundation/01-04-SUMMARY.md に結果を記録
</output>
