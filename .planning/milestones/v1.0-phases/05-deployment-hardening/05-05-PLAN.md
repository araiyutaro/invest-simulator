---
phase: 05
plan: 05
type: execute
wave: 2
depends_on: [05-01, 05-02, 05-03, 05-04]
files_modified:
  - .planning/phases/05-deployment-hardening/05-ROLLOUT.md
autonomous: false
requirements: [OPS-01, OPS-03, OPS-04]
must_haves:
  truths:
    - "ROLLOUT.md が D-15 の7ステップ初回デプロイ手順を実行可能な形に展開している"
    - "Env var 登録ステップが Vercel Dashboard UI での手動操作のみ (D-08) であり、CLI コマンドを使わない"
    - "初回 cron 発火確認 (UTC 22:00 到達後) と翌日2回目 (冪等スキップ) の検証ステップが含まれる"
    - "maxDuration=120 の実測手順 (Function Logs Duration 列) が含まれる (D-10)"
    - "各ステップに Status/Date/Notes 記録欄があり、再利用可能"
    - "05-SECURITY-CHECKLIST.md への相対リンクが存在する"
  artifacts:
    - path: ".planning/phases/05-deployment-hardening/05-ROLLOUT.md"
      provides: "Executable rollout runbook for first-time and subsequent deploys"
      contains: "## Step 1"
  key_links:
    - from: "05-ROLLOUT.md Step 4"
      to: "app/api/cron/daily-run/route.ts (Plan 05-01 GET handler)"
      via: "manual curl smoke test immediately after deploy"
      pattern: "curl -H \"Authorization: Bearer"
    - from: "05-ROLLOUT.md Step 5"
      to: "05-SECURITY-CHECKLIST.md"
      via: "relative link invoking the manual checklist"
      pattern: "05-SECURITY-CHECKLIST"
    - from: "05-ROLLOUT.md Step 6"
      to: "Vercel Function Logs"
      via: "wait-and-verify first auto-fire at UTC 22:00"
      pattern: "UTC 22"
---

<objective>
Phase 5 の初回本番デプロイ手順書 `05-ROLLOUT.md` を作成する。CONTEXT.md D-15 の7ステップを実行可能な形に展開し、D-10 (maxDuration 実測) と 05-SECURITY-CHECKLIST.md の呼び出しを配線する。

**Why Wave 2:** このプランは 05-01〜04 の成果物 (vercel.json, GET handler, next.config.ts headers, SECURITY-CHECKLIST) を統合する手順書。依存プランが存在しなければ書けない。

**Why checkpoint:** 実際の初回デプロイ (Step 3 git push → Vercel Production build) と Step 6 (UTC 22:00 の cron 自動発火を待つ) は人間の介入を要する。プラン自体は文書作成 (autonomous) でも、文書の検証には `checkpoint:human-action` + `checkpoint:human-verify` が必要になる。実デプロイは /gsd-execute-phase 実行時に checkpoint でユーザーと合意の上で実行する。

Output: `.planning/phases/05-deployment-hardening/05-ROLLOUT.md` + 文書内の手順に従った初回デプロイ実行 (checkpoint)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-deployment-hardening/05-CONTEXT.md
@.planning/phases/05-deployment-hardening/05-RESEARCH.md
@.planning/phases/05-deployment-hardening/05-SECURITY-CHECKLIST.md
@.planning/phases/05-deployment-hardening/05-01-PLAN.md
@.planning/phases/05-deployment-hardening/05-02-PLAN.md
@.planning/phases/05-deployment-hardening/05-03-PLAN.md
@.env.example
@vercel.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 05-ROLLOUT.md runbook</name>
  <files>.planning/phases/05-deployment-hardening/05-ROLLOUT.md</files>
  <read_first>
    - .planning/phases/05-deployment-hardening/05-CONTEXT.md (D-15 seven steps)
    - .planning/phases/05-deployment-hardening/05-RESEARCH.md (§Pattern 3 maxDuration fluid compute, §Pattern 8 file placement)
    - .planning/phases/05-deployment-hardening/05-SECURITY-CHECKLIST.md (created in Plan 05-04)
    - .env.example (variable inventory for Step 2 Dashboard registration)
  </read_first>
  <action>
    Create `.planning/phases/05-deployment-hardening/05-ROLLOUT.md` with EXACT content below:

    ```markdown
    ---
    phase: 05
    artifact: ROLLOUT
    reusable: true
    first_run: TBD
    status: template
    ---

    # Phase 05 — Deployment Rollout Runbook

    Executable 7-step rollout for Vercel Production deploy. Follow D-15 literally.
    Reusable on subsequent deploys (steps 1-2 only on first deploy; 3-7 every time).

    **Prereqs:**
    - [x] Plan 05-01 committed (vercel.json + daily-run GET handler)
    - [x] Plan 05-02 committed (next.config.ts security headers)
    - [x] Plan 05-03 committed (proxy.ts matcher + .env.example)
    - [x] Plan 05-04 committed ([05-SECURITY-CHECKLIST.md](./05-SECURITY-CHECKLIST.md))

    ---

    ## Step 1 — Link Vercel project (first deploy only)

    **Action (dashboard-only — D-08):**
    1. Open https://vercel.com/new
    2. "Import Git Repository" → select `invest-simulator` repo
    3. Framework Preset: Next.js (auto-detected)
    4. Root Directory: `./` (default)
    5. Build Command: default (`next build`)
    6. Do NOT click "Deploy" yet — go to Step 2 first

    **Do NOT:**
    - Run `vercel link` CLI (D-08: dashboard only)
    - Enable Vercel Deployment Protection (D-06: iron-session only)

    **Status:** ⬜ pending
    **Date:** —

    ---

    ## Step 2 — Register environment variables (first deploy, and on rotation)

    **Action:** Vercel Dashboard → Project → Settings → Environment Variables.
    Register each variable below. Scope them per this table:

    | Variable | Production | Preview | Development |
    |---|---|---|---|
    | `DATABASE_URL` | ✓ | ✓ (shared Neon, D-05) | ✗ |
    | `GEMINI_API_KEY` | ✓ | ✓ | ✗ |
    | `SESSION_SECRET` | ✓ | ✓ (same value all envs, D-06) | ✗ |
    | `SITE_PASSWORD` | ✓ | ✓ | ✗ |
    | `CRON_SECRET` | **✓ ONLY** | ✗ (Pitfall 8) | ✗ |
    | `FINNHUB_API_KEY` | ✓ | ✓ | ✗ |

    Do NOT register `DATABASE_URL_DIRECT` in Vercel — it is local-migrate only
    (see .env.example comment).

    **Secret generation (local machine, before pasting into Dashboard):**
    ```bash
    openssl rand -base64 48   # SESSION_SECRET
    openssl rand -hex 32      # CRON_SECRET
    ```

    **Verification inside Dashboard:** After saving, list should show all 6 vars
    with correct scope badges.

    **Do NOT:**
    - Use `vercel env add` CLI (D-08)
    - Enable CRON_SECRET on Preview (Pitfall 8)
    - Prefix anything with `NEXT_PUBLIC_` (D-21)

    **Status:** ⬜ pending
    **Date:** —
    **Notes:** record the last-rotation date for SESSION_SECRET here (D-09 annual rotation)

    ---

    ## Step 3 — Commit + push Phase 5 changes

    **Action:**
    ```bash
    # From repo root; expects all Plan 05-01/02/03/04 commits already on the branch
    git status
    git log --oneline -10
    git push origin master
    ```

    **Expect:**
    - Vercel Dashboard → Deployments → new "Building" row appears
    - Build log shows "Detected `vercel.json` with 1 cron job: `/api/cron/daily-run` @ `0 22 * * *`"
    - Build completes with status "Ready"
    - A production URL is assigned

    **If build fails:**
    - Check build log for env var zod errors (missing var in Dashboard → fix Step 2)
    - Check for `vercel.json` schema errors (fix JSON, re-push)

    **Status:** ⬜ pending
    **Date:** —
    **Production URL:** `https://<your-domain>.vercel.app`

    ---

    ## Step 4 — Manual curl smoke test (immediately after deploy)

    **Action:**
    ```bash
    export DOMAIN="https://<your-domain>.vercel.app"
    export CRON_SECRET="<paste from Vercel Dashboard>"

    # 4a. Unauthenticated dashboard → redirect to /login
    curl -s -o /dev/null -w "dashboard: %{http_code} → %{redirect_url}\n" "$DOMAIN/dashboard"

    # 4b. /login is accessible
    curl -s -o /dev/null -w "login: %{http_code}\n" "$DOMAIN/login"

    # 4c. Cron GET with no auth → 401
    curl -s -o /dev/null -w "cron-no-auth: %{http_code}\n" "$DOMAIN/api/cron/daily-run"

    # 4d. Cron GET with correct bearer → 200 (runs the daily cycle ONCE manually)
    curl -i -H "Authorization: Bearer $CRON_SECRET" "$DOMAIN/api/cron/daily-run"
    ```

    **Expect:**
    - `dashboard: 307 → https://<domain>/login`
    - `login: 200`
    - `cron-no-auth: 401`
    - `4d` → HTTP/2 200 + JSON body like `{"status":"success","decisionId":"...","trades":N,...}`
      OR (if same-day re-run) `{"status":"skipped","reason":"already_ran_today"}`

    **Then verify DB state:**
    ```bash
    psql "$DATABASE_URL_DIRECT" -c "SELECT run_date, portfolio_id, cost_usd FROM decisions ORDER BY created_at DESC LIMIT 3;"
    ```
    Expect: today's run_date row present with a non-null decision row.

    **If 4d returns 405:** Plan 05-01 Task 2 refactor failed. Roll back and re-check
    daily-run/route.ts GET export.

    **Status:** ⬜ pending
    **Date:** —
    **Notes:** —

    ---

    ## Step 5 — Run SECURITY-CHECKLIST manually

    **Action:** Open [05-SECURITY-CHECKLIST.md](./05-SECURITY-CHECKLIST.md) and execute
    items §1-§7 top-to-bottom. Record Status/Date/Notes in that file.

    **Expect:** items 1, 2, 3, 4, 5, 7 all ✅; item 6 ⚠️ accepted-risk.

    **Sign-off:** if any item (other than §6) is ❌, roll back the deploy or fix
    forward before proceeding to Step 6.

    **Status:** ⬜ pending
    **Date:** —

    ---

    ## Step 6 — Wait for first automatic cron fire (UTC 22:00)

    **Action:** Wait until the next UTC 22:00 (= JST 07:00). Vercel Hobby has ±59 min
    delivery precision (Pitfall 2) → actual fire time is any time between 22:00:00 and
    22:59:59 UTC. Do not treat "not exactly 22:00" as a bug.

    **After wall-clock 23:00 UTC, verify:**
    1. Vercel Dashboard → Project → Logs → filter `requestPath:/api/cron/daily-run`
    2. A new invocation row is present with User-Agent `vercel-cron/1.0`
    3. Status code 200
    4. **Duration column** reading — record this number (D-10, maxDuration measurement)
    5. DB query:
       ```bash
       psql "$DATABASE_URL_DIRECT" -c "SELECT run_date, portfolio_id, cost_usd FROM decisions WHERE run_date = CURRENT_DATE;"
       ```
       Expect: 1 row for today (the Step 4 manual row is same-day, so this depends on
       whether you ran Step 4 on the current or previous calendar day — check `run_date`).

    **Duration record:**
    | Date (UTC) | Duration (ms) | Notes |
    |---|---|---|
    | — | — | — |

    **If Duration > 90s (75% of maxDuration=120):** log a warning and begin tracking
    trends. If Duration > 120s → function timeout → **this is an incident**; file
    against Deferred Idea "Inngest/QStash fallback". Phase 05 does NOT implement the
    fallback (D-11).

    **If no invocation appears by 23:30 UTC:** check Vercel Dashboard → Cron Jobs page
    to confirm the job is registered; if missing, Plan 05-01 Task 3 `vercel.json`
    was not picked up on deploy → re-push.

    **Status:** ⬜ pending
    **Date:** —
    **Notes:** —

    ---

    ## Step 7 — Confirm idempotent second-day run

    **Action:** Wait until the NEXT calendar-day UTC 22:00 (+ ±59 min window). Verify:
    ```bash
    psql "$DATABASE_URL_DIRECT" -c "SELECT run_date, COUNT(*) FROM decisions GROUP BY run_date ORDER BY run_date DESC LIMIT 3;"
    ```

    **Expect:** Each run_date has exactly 1 row (COUNT = 1). If any date has 2+, the
    idempotent `ON CONFLICT DO NOTHING` (D-16) is broken → regression from Phase 3.

    **Also verify:** Vercel Cron Jobs page shows 2 successful invocations.

    **Status:** ⬜ pending
    **Date:** —

    ---

    ## Post-rollout — Accepted Risks summary

    After Step 7 ✅, record in PROJECT.md Key Decisions:

    | ID | Decision | Rationale |
    |---|---|---|
    | 05-AR-1 | Neon Free tier single-role, no DB-level RBAC | SECURITY-CHECKLIST §6; operator-level access control only |
    | 05-AR-2 | Vercel Hobby ±59 min cron precision | Accepted; JST morning run is non-time-critical |
    | 05-AR-3 | CSP uses `'unsafe-inline'` for script/style | Next.js RSC + Tailwind v4 requirement; nonce-based CSP deferred |
    | 05-AR-4 | HSTS without `preload` | Pitfall 6: preload is irrevocable; re-enable only when domain is permanent |
    | 05-AR-5 | No Inngest/QStash fallback for 120s overflow | D-11: address only on actual incident |

    ---

    ## Reusable checklist for subsequent deploys

    On every subsequent deploy (not first-time):

    1. ⬜ Re-run Step 3 (commit + push)
    2. ⬜ Re-run Step 4 (curl smoke test)
    3. ⬜ Re-run Step 5 (SECURITY-CHECKLIST)
    4. ⬜ Spot-check next cron fire in Vercel Logs (no 24h wait needed)
    5. ⬜ Skip Step 1 and Step 2 unless env vars rotated

    ---

    *Template created: Phase 05 Plan 05 (2026-04-13). Reusable for every deploy.*
    ```

    Commit: `docs(05-05): add deployment rollout runbook for Phase 5`.
  </action>
  <verify>
    <automated>test -f .planning/phases/05-deployment-hardening/05-ROLLOUT.md && wc -l .planning/phases/05-deployment-hardening/05-ROLLOUT.md | awk '{exit ($1 < 150)}'</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/05-deployment-hardening/05-ROLLOUT.md`
    - File is ≥150 lines
    - `grep -c "^## Step [1-7]" 05-ROLLOUT.md` == 7
    - `grep -c "vercel env add" 05-ROLLOUT.md` ≥ 1 (only in "Do NOT" warning)
    - `grep -c "Dashboard" 05-ROLLOUT.md` ≥ 5 (manual UI workflow emphasized)
    - `grep -c "05-SECURITY-CHECKLIST" 05-ROLLOUT.md` ≥ 1 (relative link)
    - `grep -c "Bearer" 05-ROLLOUT.md` ≥ 2 (Step 4 curl samples)
    - `grep -c "UTC 22" 05-ROLLOUT.md` ≥ 2 (Step 6 wall-clock verification)
    - `grep -c "maxDuration" 05-ROLLOUT.md` ≥ 1 (D-10 measurement linkage)
    - `grep -c "ON CONFLICT" 05-ROLLOUT.md` ≥ 1 (Step 7 idempotent verification reference)
    - `grep -c "Accepted Risks" 05-ROLLOUT.md` == 1
  </acceptance_criteria>
  <done>ROLLOUT.md committed. Ready for Plan 05-06 human checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint — review ROLLOUT.md + SECURITY-CHECKLIST.md before first deploy</name>
  <what-built>
    - vercel.json (Plan 05-01)
    - daily-run/route.ts GET handler + refactored handleDailyRun (Plan 05-01)
    - next.config.ts security headers (Plan 05-02)
    - proxy.ts matcher + .env.example (Plan 05-03)
    - 05-SECURITY-CHECKLIST.md (Plan 05-04)
    - 05-ROLLOUT.md (this plan, Task 1)
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/05-deployment-hardening/05-ROLLOUT.md` and read Steps 1-7
    2. Open `.planning/phases/05-deployment-hardening/05-SECURITY-CHECKLIST.md` and read items §1-§7
    3. Confirm you are ready to execute the rollout manually (Step 1 link + Step 2 env var registration in Vercel Dashboard)
    4. Confirm the following env vars are ready to paste:
       - DATABASE_URL (Neon pooled)
       - GEMINI_API_KEY
       - SESSION_SECRET (generate: `openssl rand -base64 48`)
       - SITE_PASSWORD
       - CRON_SECRET (generate: `openssl rand -hex 32`)
       - FINNHUB_API_KEY
    5. Decide: do you want to proceed with the actual first-time rollout right now (Plan 05-06 will orchestrate the post-deploy verification), or pause here and execute Steps 1-7 manually at your own pace later?
  </how-to-verify>
  <resume-signal>Type "approved — proceed to Plan 05-06" to continue, or "approved — I'll deploy manually later" to pause. If you find issues in the runbook, describe them and we'll revise.</resume-signal>

  <files>N/A (checkpoint task — no file edits)</files>
  <action>Checkpoint only. See <how-to-verify> and <resume-signal> below. Executor must present the verification steps to the user and pause until the resume-signal is received.</action>
  <verify>Human confirms via resume-signal.</verify>
  <done>User replies with an acceptable resume-signal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Written runbook ↔ Human operator | Runbook is the enforcement surface for D-08 (dashboard-only env registration) and Pitfall 8 (CRON_SECRET scope) |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-23 | Spoofing (wrong env scope) | CRON_SECRET leaked to Preview | mitigate | Step 2 scope table makes Production-only explicit; Pitfall 8 referenced |
| T-05-24 | Tampering (CLI env add) | Drifting between Dashboard and CLI state | mitigate | Step 1/2 "Do NOT" warnings explicitly block `vercel link` and `vercel env add` (D-08) |
| T-05-25 | Repudiation (unmeasured duration) | maxDuration blind spot | mitigate | Step 6 Duration record table creates an audit trail for D-10; tracks trend across deploys |
| T-05-26 | DoS (cron not registered) | vercel.json not picked up | mitigate | Step 6 explicit "if no invocation by 23:30 UTC" fallback check + Vercel Dashboard → Cron Jobs page lookup |
| T-05-10 | DoS (duplicate cron delivery) | decisions table double-insert | mitigate (existing) | Step 7 verifies D-16 idempotent guard; Phase 3 invariant |
</threat_model>

<verification>
1. Grep assertions from Task 1
2. Human review in Task 2 (blocking checkpoint)
3. After Plan 05-06 runs, each step of ROLLOUT.md gets Status = ✅ or ⚠️
</verification>

<success_criteria>
- 05-ROLLOUT.md exists and passes all grep checks
- Runbook references SECURITY-CHECKLIST by relative link
- Runbook explicitly forbids `vercel env add` CLI (D-08)
- Runbook explicitly scopes CRON_SECRET to Production only (Pitfall 8)
- Human confirms runbook readability before Plan 05-06 executes the deploy
</success_criteria>

<output>
After completion, create `.planning/phases/05-deployment-hardening/05-05-SUMMARY.md` listing:
- ROLLOUT.md step count + what each covers
- Link to SECURITY-CHECKLIST integration
- Checkpoint resume decision recorded by user
</output>
</content>
</invoke>