---
phase: 05
plan: 06
type: execute
wave: 3
depends_on: [05-05]
files_modified:
  - .planning/phases/05-deployment-hardening/05-SECURITY-CHECKLIST.md
  - .planning/phases/05-deployment-hardening/05-ROLLOUT.md
  - .planning/phases/05-deployment-hardening/05-VERIFICATION.md
  - .planning/PROJECT.md
autonomous: false
requirements: [OPS-01, OPS-02, OPS-03, OPS-04]
must_haves:
  truths:
    - "本番 Vercel Production URL に対して SECURITY-CHECKLIST §1-§7 が全て ✅ または ⚠️ accepted-risk 状態で記録されている"
    - "05-ROLLOUT.md の Step 3-7 の Status 欄が ✅ または ⚠️ で埋まっている"
    - "Vercel Function Logs に `/api/cron/daily-run` の invocation が1件以上記録され、Duration が120s未満 (maxDuration) であることが記録されている"
    - "decisions テーブルに本番稼働後の run_date レコードが1件存在する (OPS-01 成功基準 #1)"
    - "05-VERIFICATION.md にフェーズ完了サインオフが書き出されている"
    - "PROJECT.md Key Decisions に 05-AR-1..05-AR-5 が追記されている"
  artifacts:
    - path: ".planning/phases/05-deployment-hardening/05-VERIFICATION.md"
      provides: "Phase 5 final sign-off document with links to each evidence item"
      contains: "OPS-01"
  key_links:
    - from: "05-VERIFICATION.md OPS-01 row"
      to: "Vercel Function Logs + decisions table row"
      via: "post-deploy manual verification"
      pattern: "decisions.*run_date"
    - from: "PROJECT.md Key Decisions"
      to: "05-AR-1..05-AR-5 accepted risks"
      via: "ROLLOUT.md Post-rollout summary"
      pattern: "05-AR-"
---

<objective>
Phase 5 最終検証プラン。実デプロイを実行し、SECURITY-CHECKLIST + ROLLOUT.md の手順を全てユーザーと共に走らせ、エビデンスを記録し、05-VERIFICATION.md にフェーズサインオフを書き出す。

本プランは「実行」と「記録」だけ。新規コード・設定変更はしない。

**Wave 3 理由:** Plan 05-05 の checkpoint でユーザーが「proceed」を宣言した直後にこのプランが走る。Plan 05-05 の成果物 (ROLLOUT.md) を台帳として使う。

**Why mostly checkpoints:** Step 1-2 (Vercel link + env var) と Step 6-7 (24h 待機後の自動 cron 確認) は Claude が自動化できない。Claude は curl/psql/git 操作 (Step 3-5) を実行し、人間の待機項目 (Step 6-7) は checkpoint で待つ。

Output: 記入済み SECURITY-CHECKLIST.md + ROLLOUT.md + 新規 05-VERIFICATION.md + PROJECT.md 追記
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-deployment-hardening/05-ROLLOUT.md
@.planning/phases/05-deployment-hardening/05-SECURITY-CHECKLIST.md
@.planning/phases/05-deployment-hardening/05-VALIDATION.md
@.planning/phases/05-deployment-hardening/05-CONTEXT.md
@.planning/PROJECT.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Human — Execute ROLLOUT Step 1 (Vercel project link) + Step 2 (env var registration)</name>
  <what-built>Plans 05-01〜05-05 complete; ready to deploy.</what-built>
  <how-to-verify>
    Execute **only** ROLLOUT.md Step 1 and Step 2 manually:

    1. Open [05-ROLLOUT.md Step 1](./05-ROLLOUT.md#step-1--link-vercel-project-first-deploy-only)
    2. Import the repo into Vercel via the Dashboard UI (https://vercel.com/new)
    3. Do NOT click "Deploy" yet
    4. Open [05-ROLLOUT.md Step 2](./05-ROLLOUT.md#step-2--register-environment-variables-first-deploy-and-on-rotation)
    5. Register all 6 environment variables in Project Settings → Environment Variables:
       - `DATABASE_URL`, `GEMINI_API_KEY`, `SESSION_SECRET`, `SITE_PASSWORD`, `FINNHUB_API_KEY`: Production + Preview
       - `CRON_SECRET`: **Production ONLY** (Pitfall 8)
    6. Open ROLLOUT.md and update Step 1/Step 2 Status blocks to ✅ with today's date
    7. Return the Vercel Production URL (e.g., `https://invest-simulator-abc.vercel.app`)

    **This is a truly human-only step:** Vercel Dashboard UI has no safe automated
    equivalent under D-08 (locked: no `vercel link` / `vercel env add` CLI).
  </how-to-verify>
  <resume-signal>
    Paste the Vercel Production URL (e.g., `https://invest-simulator-xxx.vercel.app`) and confirm env vars are registered. Reply format: `DOMAIN=https://... — env vars registered`.
  </resume-signal>

  <files>N/A (checkpoint task — no file edits)</files>
  <action>Checkpoint only. See <how-to-verify> and <resume-signal> below. Executor must present the verification steps to the user and pause until the resume-signal is received.</action>
  <verify>Human confirms via resume-signal.</verify>
  <done>User replies with an acceptable resume-signal.</done>
</task>

<task type="auto">
  <name>Task 2: Execute ROLLOUT Step 3 — commit + push Phase 5</name>
  <files>.planning/phases/05-deployment-hardening/05-ROLLOUT.md</files>
  <read_first>
    - .planning/phases/05-deployment-hardening/05-ROLLOUT.md (Step 3)
  </read_first>
  <action>
    Run:
    ```bash
    git status
    git log --oneline -15
    git push origin master
    ```

    Then open Vercel Dashboard → Deployments and wait for the "Building" row to
    become "Ready". Record:
    - Deployment ID (or git SHA) under ROLLOUT.md Step 3 Notes
    - Update Step 3 Status to ✅ with today's date
    - Confirm build log contains `Detected "crons" ... /api/cron/daily-run`

    If build fails:
    - Read Vercel build log
    - Roll forward (fix env var, fix vercel.json, etc.) or roll back
    - Do NOT proceed until Ready

    Commit: `docs(05-06): record Step 3 deploy in ROLLOUT.md`.
  </action>
  <verify>
    <automated>grep -A2 "^## Step 3" .planning/phases/05-deployment-hardening/05-ROLLOUT.md | grep -c "Status:.*✅" | awk '{exit ($1 < 1)}'</automated>
  </verify>
  <acceptance_criteria>
    - Step 3 Status in ROLLOUT.md is ✅
    - Commit recording the Step 3 status exists
    - Vercel Dashboard shows production deployment "Ready" (human confirms via log grep or visual)
  </acceptance_criteria>
  <done>Production deployment live; ready for smoke test.</done>
</task>

<task type="auto">
  <name>Task 3: Execute ROLLOUT Step 4 + Step 5 — curl smoke + SECURITY-CHECKLIST manual run</name>
  <files>.planning/phases/05-deployment-hardening/05-ROLLOUT.md, .planning/phases/05-deployment-hardening/05-SECURITY-CHECKLIST.md</files>
  <read_first>
    - .planning/phases/05-deployment-hardening/05-ROLLOUT.md (Step 4 + Step 5)
    - .planning/phases/05-deployment-hardening/05-SECURITY-CHECKLIST.md (items §1-§7)
  </read_first>
  <action>
    Export `DOMAIN` and `CRON_SECRET` from Task 1 checkpoint return value. Then execute
    the full curl/grep/psql sequence from ROLLOUT.md Step 4:

    ```bash
    export DOMAIN="https://<from-task-1>"
    export CRON_SECRET="<from-task-1>"

    # 4a
    curl -s -o /dev/null -w "dashboard: %{http_code} → %{redirect_url}\n" "$DOMAIN/dashboard"
    # 4b
    curl -s -o /dev/null -w "login: %{http_code}\n" "$DOMAIN/login"
    # 4c
    curl -s -o /dev/null -w "cron-no-auth: %{http_code}\n" "$DOMAIN/api/cron/daily-run"
    # 4d (the critical one)
    curl -i -H "Authorization: Bearer $CRON_SECRET" "$DOMAIN/api/cron/daily-run"
    ```

    Record each result in ROLLOUT.md Step 4 Notes. Expected:
    - `dashboard: 307 → .../login`
    - `login: 200`
    - `cron-no-auth: 401`
    - `4d` → 200 + JSON with decisionId

    Verify DB:
    ```bash
    psql "$DATABASE_URL_DIRECT" -c "SELECT run_date, portfolio_id, cost_usd FROM decisions ORDER BY created_at DESC LIMIT 3;"
    ```

    **If 4d returns 405:** STOP. Plan 05-01 Task 2 regression. File incident, roll back.

    **Then run all 7 items in SECURITY-CHECKLIST.md:**
    - §1: `git check-ignore -v .env .env.local .env.production`
    - §2: loop curl over `/`, `/dashboard`, `/api/dashboard/timeline?...`
    - §3: curl 4 variants (no auth, wrong bearer, POST wrong, correct bearer)
    - §4: `grep -n "matcher" proxy.ts`
    - §5: Vercel Dashboard Logs → CSV export → grep secrets (manual; record CSV filename + grep result)
    - §6: accepted-risk (just confirm `neondb_owner` via `psql -c "\\du"`)
    - §7: `curl -sI "$DOMAIN/login" | grep -iE "^(strict-...)"`

    Update each Status block in SECURITY-CHECKLIST.md to ✅ / ⚠️ / ❌ with today's
    date and any notes. Commit: `docs(05-06): record Step 4-5 verification outputs`.

    **Block on:** any ❌ (fail) in items §1-§5 or §7. §6 must be ⚠️ (expected).
  </action>
  <verify>
    <automated>grep -c "Status: ✅" .planning/phases/05-deployment-hardening/05-SECURITY-CHECKLIST.md | awk '{exit ($1 < 6)}'</automated>
  </verify>
  <acceptance_criteria>
    - ROLLOUT.md Step 4 Notes contain all 4 curl outputs (4a-4d)
    - ROLLOUT.md Step 4 Status is ✅
    - ROLLOUT.md Step 5 Status is ✅
    - SECURITY-CHECKLIST.md items §1, §2, §3, §4, §5, §7 have Status ✅
    - SECURITY-CHECKLIST.md item §6 has Status ⚠️ accepted-risk
    - No ❌ status anywhere in SECURITY-CHECKLIST.md
    - `decisions` table has a row with today's `run_date` (captured in Step 4 psql output and recorded in ROLLOUT.md Notes)
  </acceptance_criteria>
  <done>All structural & runtime verifications complete; only automatic cron-fire wait remains.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Checkpoint — Wait for first automatic cron fire (Step 6) + second-day idempotent run (Step 7)</name>
  <what-built>
    Production deployment live, curl smoke + SECURITY-CHECKLIST complete (Tasks 2-3).
    Remaining: observe Vercel Cron auto-fire at UTC 22:00 (D-10 Duration measurement)
    and verify idempotent second-day run.
  </what-built>
  <how-to-verify>
    **Immediate (next UTC 22:00 window):**
    1. Wait for the next UTC 22:00 to 22:59 window (JST 07:00-07:59)
    2. After UTC 23:00, open Vercel Dashboard → Project → Logs
    3. Filter by `requestPath:/api/cron/daily-run`
    4. Confirm new invocation row with `user-agent: vercel-cron/1.0` and status 200
    5. Record Duration (ms) in ROLLOUT.md Step 6 Duration table
    6. Run DB query:
       ```bash
       psql "$DATABASE_URL_DIRECT" -c "SELECT run_date, portfolio_id, cost_usd FROM decisions WHERE run_date = CURRENT_DATE;"
       ```
    7. Update ROLLOUT.md Step 6 Status to ✅

    **24h later (Step 7):**
    1. Wait for the next-day UTC 22:00 window
    2. Re-run:
       ```bash
       psql "$DATABASE_URL_DIRECT" -c "SELECT run_date, COUNT(*) FROM decisions GROUP BY run_date ORDER BY run_date DESC LIMIT 3;"
       ```
    3. Confirm each run_date has COUNT = 1 (idempotent D-16)
    4. Confirm Vercel Cron Jobs page shows 2 successful invocations
    5. Update ROLLOUT.md Step 7 Status to ✅

    **Duration threshold check:**
    - If Duration < 90s: ✅ green, no action
    - If Duration 90-120s: ⚠️ warning, note in Step 6 Notes; monitor trend
    - If Duration ≥ 120s: ❌ incident, file against Deferred "Inngest/QStash fallback" (D-11)

    This task intrinsically requires wall-clock waiting (Vercel Cron cannot be fired
    locally — Pitfall 7). Do NOT synthesize data; report actual observed values.

    **Reply with:**
    - Step 6 Duration (ms)
    - Step 7 per-run_date COUNT values
    - Status for both steps (✅ / ⚠️ / ❌)
  </how-to-verify>
  <resume-signal>
    Reply format: `Step 6: duration=XXXXms status=✅ | Step 7: idempotent=pass status=✅`. If either step failed, describe what happened and we will open a gap closure plan.
  </resume-signal>

  <files>N/A (checkpoint task — no file edits)</files>
  <action>Checkpoint only. See <how-to-verify> and <resume-signal> below. Executor must present the verification steps to the user and pause until the resume-signal is received.</action>
  <verify>Human confirms via resume-signal.</verify>
  <done>User replies with an acceptable resume-signal.</done>
</task>

<task type="auto">
  <name>Task 5: Write 05-VERIFICATION.md + update PROJECT.md with accepted risks</name>
  <files>.planning/phases/05-deployment-hardening/05-VERIFICATION.md, .planning/PROJECT.md</files>
  <read_first>
    - .planning/phases/05-deployment-hardening/05-ROLLOUT.md (all Step Status values post-checkpoint)
    - .planning/phases/05-deployment-hardening/05-SECURITY-CHECKLIST.md (filled-in statuses)
    - .planning/PROJECT.md (Key Decisions section for append)
    - .planning/phases/01-foundation/01-VERIFICATION.md (format reference)
  </read_first>
  <action>
    **Part A — Create 05-VERIFICATION.md:**

    Write `.planning/phases/05-deployment-hardening/05-VERIFICATION.md`:

    ```markdown
    ---
    phase: 05
    artifact: VERIFICATION
    status: complete
    signed_off: <today-date>
    ---

    # Phase 05 — Final Verification

    Signed off on <today-date> after full rollout + 24h observation window.

    ## Requirement Coverage

    | Req | Evidence | Status |
    |---|---|---|
    | OPS-01 | Vercel Cron auto-fired at UTC 22:00 window; `decisions` table row for run_date = <date>; Duration = <XXX>ms (<120s maxDuration) — see ROLLOUT.md Step 6 | ✅ |
    | OPS-02 | SECURITY-CHECKLIST §3: GET/POST no-header, wrong-bearer, correct-bearer all return expected 401/200 | ✅ |
    | OPS-03 | SECURITY-CHECKLIST §2: /dashboard → 307 /login; /api/dashboard/* → 401; SECURITY-CHECKLIST §7: 6 security headers returned | ✅ |
    | OPS-04 | ROLLOUT.md Step 6 Duration table records actual Function Log Duration; under 120s maxDuration threshold | ✅ / ⚠️ (fill) |

    ## Phase 5 Success Criteria (from ROADMAP)

    | # | Criterion | Evidence |
    |---|---|---|
    | 1 | Vercel Cron で市場クローズ後に毎日自動で日次サイクルを実行し `decisions` に新行 | ROLLOUT.md Step 6 + Step 7 records (2 consecutive run_date rows, COUNT=1 each) |
    | 2 | CRON_SECRET なしで `/api/cron/daily-run` → 401 | SECURITY-CHECKLIST §3 output |
    | 3 | 未認証でダッシュボードアクセス → /login redirect、API は 401 | SECURITY-CHECKLIST §2 output |
    | 4 | `maxDuration=120` 設定済、超過リスクがある場合はフォールバック方針存在 | daily-run/route.ts L25 preserved; Duration measurement recorded; D-11 fallback-on-incident policy documented in ROLLOUT.md Accepted Risks |

    ## STRIDE Mitigation Closure

    | Threat | Plan | Mitigation verified |
    |---|---|---|
    | T-05-01 Spoofing /cron | 05-01 | Unit test + SECURITY-CHECKLIST §3 (4 curl variants) |
    | T-05-02 MITM downgrade | 05-02 | SECURITY-CHECKLIST §7 HSTS header |
    | T-05-03 NEXT_PUBLIC_ secret exposure | 05-03 | .env.example warning + audit |
    | T-05-05 405 infinite loop | 05-01 | GET handler added; SECURITY-CHECKLIST §3 correct-bearer returns 200 |
    | T-05-06 Secrets in logs | 05-04 | SECURITY-CHECKLIST §5 grep CSV |
    | T-05-11 Clickjacking | 05-02 | X-Frame-Options + frame-ancestors 'none' |
    | T-05-12 MIME sniff | 05-02 | X-Content-Type-Options nosniff |
    | T-05-13 Referrer leak | 05-02 | Referrer-Policy strict-origin-when-cross-origin |
    | T-05-14 XSS (partial) | 05-02 | CSP default-src 'self'; `'unsafe-inline'` accepted-risk |
    | T-05-15 Permissions abuse | 05-02 | Permissions-Policy camera/mic/geo disabled |
    | T-05-17 Matcher overexclusion | 05-03 | proxy.ts matcher grep (SECURITY-CHECKLIST §4) |
    | T-05-19 CRON_SECRET Preview leak | 05-03 | .env.example "PRODUCTION ONLY" note + Vercel Dashboard scope |
    | T-05-20 Silent regression | 05-04 | SECURITY-CHECKLIST is reusable for every deploy |

    ## Accepted Risks

    Recorded in PROJECT.md Key Decisions as 05-AR-1 … 05-AR-5 (see ROLLOUT.md Post-rollout).

    ## Sign-off

    Phase 5 complete: <today-date>. Next action: mark ROADMAP Phase 5 ☑ and close.
    ```

    Fill in `<today-date>` and actual Duration / Status values from the checkpoint reply.

    **Part B — Append to PROJECT.md Key Decisions:**

    Open `.planning/PROJECT.md`, locate the Key Decisions section, append:

    ```markdown
    | 05-AR-1 | Neon Free tier single-role (no DB RBAC) | Phase 5 | Accepted; operator-level access control sufficient for personal project |
    | 05-AR-2 | Vercel Hobby ±59 min cron precision | Phase 5 | Accepted; JST morning run is non-time-critical |
    | 05-AR-3 | CSP uses `'unsafe-inline'` for script/style | Phase 5 | Next.js RSC + Tailwind v4 requirement; nonce-based CSP deferred |
    | 05-AR-4 | HSTS without `preload` | Phase 5 | Pitfall 6: preload is irrevocable; re-enable when domain permanent |
    | 05-AR-5 | No Inngest/QStash fallback for 120s overflow | Phase 5 | D-11: address only on actual incident |
    ```

    (Match the existing table structure; if the existing table has different columns,
    adapt but preserve all 5 row identifiers 05-AR-1..05-AR-5.)

    Commit: `docs(05-06): write phase 5 verification + record accepted risks`.
  </action>
  <verify>
    <automated>test -f .planning/phases/05-deployment-hardening/05-VERIFICATION.md && grep -c "05-AR-" .planning/PROJECT.md | awk '{exit ($1 < 5)}'</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/05-deployment-hardening/05-VERIFICATION.md` exists
    - `grep -c "OPS-0[1-4]" 05-VERIFICATION.md` == 4
    - `grep -c "✅" 05-VERIFICATION.md` ≥ 4 (requirement coverage row per requirement)
    - `grep -c "05-AR-[1-5]" .planning/PROJECT.md` ≥ 5
    - `grep -c "Signed off" 05-VERIFICATION.md` ≥ 1
    - Both files committed
  </acceptance_criteria>
  <done>Phase 5 formally signed off; accepted risks recorded in project-level decisions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Executing plans 05-01..05-05 ↔ Actual production deployment | Verification gap — code/config exists in git but not yet running on a real Vercel URL |
| Observed behavior ↔ Project memory | 05-VERIFICATION.md is the evidence trail linking each requirement to a recorded test output |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-27 | Repudiation (unverified deploy) | Phase closed without real evidence | mitigate | This plan forces actual curl/psql/Vercel Log observation before sign-off; Task 4 checkpoint blocks on wall-clock cron evidence |
| T-05-28 | Tampering (Synthetic data) | Claude could fabricate Duration numbers | mitigate | Task 4 explicitly requires wall-clock wait (Pitfall 7) and user-reported values; Task 5 inserts user's values verbatim |
| T-05-06 | Information Disclosure (logs) | SECURITY-CHECKLIST §5 not run on real logs | mitigate | Task 3 runs §5 against real Vercel Dashboard log export |
| T-05-10 | DoS (dup cron) | Idempotent guard untested in production | mitigate | Task 4 Step 7 verifies COUNT=1 per run_date across 2 days |
</threat_model>

<verification>
Phase 5 is verified when this plan's Task 5 commits 05-VERIFICATION.md with all
requirement rows set to ✅ and Task 4 checkpoint returned actual Duration + COUNT
values from Vercel / Neon.
</verification>

<success_criteria>
- Plan 05-01..05-05 artifacts are demonstrably live on production Vercel URL
- SECURITY-CHECKLIST.md items 1-5, 7 all ✅; item 6 ⚠️ accepted-risk
- ROLLOUT.md Steps 1-7 all ✅ (or ⚠️ with recorded rationale)
- 05-VERIFICATION.md signed off with actual measured values (not TBD)
- PROJECT.md Key Decisions records 05-AR-1..05-AR-5
- OPS-01, OPS-02, OPS-03, OPS-04 all marked complete in REQUIREMENTS.md traceability (separate quick-fix commit OK)
</success_criteria>

<output>
After completion, create `.planning/phases/05-deployment-hardening/05-06-SUMMARY.md` listing:
- Measured Duration value from Vercel Logs
- Per-day decisions COUNT verifying idempotency
- Link to 05-VERIFICATION.md sign-off
- Phase 5 complete marker for ROADMAP.md update
</output>
</content>
</invoke>