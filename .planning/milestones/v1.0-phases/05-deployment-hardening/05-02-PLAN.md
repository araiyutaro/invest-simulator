---
phase: 05
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - next.config.ts
autonomous: true
requirements: [OPS-03]
must_haves:
  truths:
    - "すべての HTTP レスポンスに Strict-Transport-Security / X-Content-Type-Options / Referrer-Policy / X-Frame-Options / Permissions-Policy / Content-Security-Policy の6ヘッダが返る"
    - "CSP は `'self'` ベースで、Next.js App Router + Tailwind v4 の inline style を壊さないよう `'unsafe-inline'` を含む"
    - "HSTS は `preload` を含まない（Pitfall 6: 撤回不可を回避）"
    - "next.config.ts の `async headers()` 経由で配線され、proxy.ts / vercel.json には重複しない"
  artifacts:
    - path: "next.config.ts"
      provides: "securityHeaders array + async headers() mapping"
      exports: ["nextConfig (default)"]
      contains: "async headers"
  key_links:
    - from: "next.config.ts async headers()"
      to: "全ルート (source: '/:path*')"
      via: "Next.js build-time header injection"
      pattern: "source: '/:path\\*'"
---

<objective>
CSP を含む6種のセキュリティヘッダを `next.config.ts` の `async headers()` で全ルートに静的配線する。これは D-13 §7 の受け入れ基準であり、OPS-03 (どこからでもアクセスできる本番デプロイ = HTTPS + セキュリティ基準) の構成要素。

**Why next.config.ts:** RESEARCH.md §Pattern 4 より、3択 (next.config.ts vs proxy.ts vs vercel.json) の canonical は `next.config.ts` の `async headers()`。Next.js 公式 CSP ガイドが「nonce 不要ならこれを使え」と明示。proxy.ts に入れると認証ゲートとヘッダ付与の責務が混ざる。vercel.json に入れると Next.js との二重管理で保守しにくい。

**Why 'unsafe-inline' 残置:** Pitfall 5 より、Next.js RSC + Tailwind v4 は build 時 inline `<style>` を生成するため nonce 方式に移行しない限り必須。nonce 方式は dynamic rendering 化が必要で Phase 5 スコープ外。

Output: `next.config.ts` に securityHeaders 定数と async headers() を追加。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-deployment-hardening/05-CONTEXT.md
@.planning/phases/05-deployment-hardening/05-RESEARCH.md
@next.config.ts

<interfaces>
<!-- From next.config.ts (current state) -->
```ts
import type { NextConfig } from "next";
const nextConfig: NextConfig = { /* empty */ };
export default nextConfig;
```

<!-- From node_modules/next/dist/docs/01-app/03-api-reference/05-config/01-next-config-js/headers.md -->
```ts
type HeadersFn = () => Promise<Array<{
  source: string;
  headers: Array<{ key: string; value: string }>;
}>>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add securityHeaders + async headers() to next.config.ts</name>
  <files>next.config.ts</files>
  <read_first>
    - next.config.ts (current content)
    - .planning/phases/05-deployment-hardening/05-RESEARCH.md (§Pattern 4 full code, §Pitfall 5, §Pitfall 6)
    - .planning/phases/05-deployment-hardening/05-CONTEXT.md (D-13 §7)
  </read_first>
  <action>
    Replace `next.config.ts` entirely with the following exact content:

    ```ts
    import type { NextConfig } from "next";

    // Phase 05 Plan 02 / D-13 §7: global security headers
    // Wiring: next.config.ts async headers() — canonical per Next.js CSP guide
    // [CITED: node_modules/next/dist/docs/01-app/02-guides/content-security-policy.md]
    // [CITED: node_modules/next/dist/docs/01-app/03-api-reference/05-config/01-next-config-js/headers.md]
    const securityHeaders = [
      {
        // HSTS without `preload` — Pitfall 6: preload is irrevocable
        key: "Strict-Transport-Security",
        value: "max-age=63072000; includeSubDomains",
      },
      {
        key: "X-Content-Type-Options",
        value: "nosniff",
      },
      {
        key: "Referrer-Policy",
        value: "strict-origin-when-cross-origin",
      },
      {
        key: "X-Frame-Options",
        value: "DENY",
      },
      {
        key: "Permissions-Policy",
        value: "camera=(), microphone=(), geolocation=()",
      },
      {
        // CSP: 'unsafe-inline' required for Next.js App Router + Tailwind v4
        // inline <style> injection (Pitfall 5). nonce-based CSP is deferred
        // because it requires dynamic rendering — out of Phase 5 scope.
        key: "Content-Security-Policy",
        value: [
          "default-src 'self'",
          "script-src 'self' 'unsafe-inline'",
          "style-src 'self' 'unsafe-inline'",
          "img-src 'self' data: blob:",
          "font-src 'self' data:",
          "connect-src 'self'",
          "frame-ancestors 'none'",
          "base-uri 'self'",
          "form-action 'self'",
          "object-src 'none'",
          "upgrade-insecure-requests",
        ].join("; "),
      },
    ];

    const nextConfig: NextConfig = {
      async headers() {
        return [
          {
            source: "/:path*",
            headers: securityHeaders,
          },
        ];
      },
    };

    export default nextConfig;
    ```

    After edit, run `pnpm tsc --noEmit && pnpm next build`. Build must succeed. Do a smoke test with `pnpm next start` in background + `curl -sI http://localhost:3000/login | grep -iE "^(strict-transport-security|x-content-type-options|referrer-policy|x-frame-options|permissions-policy|content-security-policy)"` → should return 6 lines. Kill background server. Commit: `feat(05-02): add global security headers via next.config.ts`.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit && pnpm next build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "securityHeaders" next.config.ts` ≥ 2
    - `grep -c "async headers" next.config.ts` == 1
    - `grep -c "Strict-Transport-Security" next.config.ts` == 1
    - `grep -c "max-age=63072000; includeSubDomains" next.config.ts` == 1
    - `grep "preload" next.config.ts` returns 0 matches (HSTS preload NOT present — Pitfall 6)
    - `grep -c "X-Content-Type-Options" next.config.ts` == 1
    - `grep -c "Referrer-Policy" next.config.ts` == 1
    - `grep -c "X-Frame-Options" next.config.ts` == 1
    - `grep -c "Permissions-Policy" next.config.ts` == 1
    - `grep -c "Content-Security-Policy" next.config.ts` == 1
    - `grep -c "unsafe-inline" next.config.ts` ≥ 2 (script-src + style-src)
    - `grep -c "frame-ancestors 'none'" next.config.ts` == 1
    - `grep -c "source: \"/:path\\*\"" next.config.ts` == 1
    - `pnpm tsc --noEmit` exits 0
    - `pnpm next build` exits 0
  </acceptance_criteria>
  <done>next.config.ts returns 6 security headers for every route at build time.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public internet → Next.js response | HTTP responses cross the trust boundary; headers instruct browsers on allowed behaviors |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | Tampering (MITM downgrade) | HTTPS transport | mitigate | `Strict-Transport-Security: max-age=63072000; includeSubDomains` forces HTTPS for 2 years on every subdomain; verified by curl -I grep in Plan 05-04 SECURITY-CHECKLIST §7 |
| T-05-11 | Tampering (clickjacking) | /login, /dashboard iframe embedding | mitigate | `X-Frame-Options: DENY` + CSP `frame-ancestors 'none'` block all iframe embedding |
| T-05-12 | Information Disclosure (MIME sniffing) | all responses | mitigate | `X-Content-Type-Options: nosniff` prevents browser from reinterpreting content-type |
| T-05-13 | Information Disclosure (referrer leakage) | cross-origin navigations | mitigate | `Referrer-Policy: strict-origin-when-cross-origin` strips path/query on cross-origin |
| T-05-14 | XSS via injected script | dashboard pages rendering Gemini transcripts | mitigate (partial) | CSP `default-src 'self'` blocks remote scripts; `script-src 'self' 'unsafe-inline'` still allows inline (accepted risk — nonce-based CSP deferred per Pitfall 5); `object-src 'none'` blocks Flash/plugin vectors |
| T-05-15 | Abuse of browser APIs | camera/mic/geolocation from injected code | mitigate | `Permissions-Policy: camera=(), microphone=(), geolocation=()` disables unused powerful APIs |
| T-05-16 | HSTS lock-in (operational risk) | HSTS preload irrevocable | accept | `preload` directive intentionally omitted (Pitfall 6); re-enable only after domain is production-stable and DNS is finalized |
</threat_model>

<verification>
1. `pnpm tsc --noEmit` — exits 0
2. `pnpm next build` — exits 0
3. Static grep assertions (see acceptance_criteria)
4. Manual post-deploy curl (documented in Plan 05-04 SECURITY-CHECKLIST §7):
   ```
   curl -sI https://<domain>/login | grep -iE "^(strict-transport-security|x-content-type-options|referrer-policy|content-security-policy|x-frame-options|permissions-policy)"
   ```
   → 6 lines expected
5. Visual dashboard smoke after deploy: Open Chrome DevTools Console on `/dashboard` — MUST NOT show `Refused to execute inline script because it violates the following Content Security Policy directive` (Pitfall 5 regression detector)
</verification>

<success_criteria>
- next.config.ts exports 6 security headers for `/:path*`
- Build and type-check pass
- HSTS has NO preload directive
- CSP includes `'unsafe-inline'` for script-src and style-src (Next.js/Tailwind compatibility) but blocks remote scripts via `default-src 'self'`
- Security header configuration exists in exactly ONE place (next.config.ts) — not duplicated in proxy.ts or vercel.json
</success_criteria>

<output>
After completion, create `.planning/phases/05-deployment-hardening/05-02-SUMMARY.md` listing each header with its value and a note linking to Pitfall 5/6 in RESEARCH.md.
</output>
</content>
</invoke>