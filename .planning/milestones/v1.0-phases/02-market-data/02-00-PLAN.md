---
phase: 02-market-data
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - scripts/spike-raw-close.ts
  - .planning/phases/02-market-data/02-SPIKE-RAW-CLOSE.md
  - lib/__tests__/fixtures/market/yahoo-chart-aapl.json
  - lib/__tests__/fixtures/market/yahoo-chart-7203-T.json
  - lib/__tests__/fixtures/market/finnhub-news-aapl.json
  - lib/__tests__/fixtures/market/finnhub-basicfinancials-aapl.json
  - lib/__tests__/fixtures/market/stooq-7203-jp.csv
  - lib/__tests__/fixtures/market/stooq-error.html
autonomous: true
requirements: []
must_haves:
  truths:
    - "SPIKE executed against real yahoo-finance2 and Stooq for a known split date"
    - "raw_close semantics decided: option (a) or (b) only — option (c) dual-source is OUT OF SCOPE for Phase 2"
    - "Test fixtures captured for yahoo, Finnhub, Stooq so Wave 1+ tests run offline"
  artifacts:
    - path: scripts/spike-raw-close.ts
      provides: "One-shot runner that fetches yahoo chart() + Stooq CSV for TSLA 2022-08-25 or AAPL 2020-08-31 and prints close-value diff"
    - path: .planning/phases/02-market-data/02-SPIKE-RAW-CLOSE.md
      provides: "Decision record: which of D-08 (a)/(b)/(c) is chosen + rationale + numeric evidence"
    - path: lib/__tests__/fixtures/market/yahoo-chart-aapl.json
      provides: "Captured yahoo-finance2 chart() response for AAPL used as offline unit-test fixture"
  key_links:
    - from: scripts/spike-raw-close.ts
      to: yahoo-finance2 chart() + Stooq CSV endpoint
      via: live network call (run locally, not in Vercel)
      pattern: "yahooFinance\\.chart\\(|stooq\\.com/q/d/l"
---

<objective>
Wave 0 blocking SPIKE and fixture capture. Resolve the D-08 `raw_close` semantics open question via real-data experiment, and capture offline fixtures so every subsequent wave's unit tests run without network. No Wave 1 task may touch `raw_close` logic until this plan completes and `02-SPIKE-RAW-CLOSE.md` is committed.

Purpose: Amendment A left D-08 with three candidates ((a) yahoo adjusted = raw, (b) NULL unless Stooq dual-source, (c) Stooq as primary raw). We cannot pick safely without observed numeric behavior on a split date.
Output: Decision doc + spike script + 6 fixture files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-market-data/02-CONTEXT.md
@.planning/phases/02-market-data/02-RESEARCH.md
@.planning/research/PITFALLS.md
@db/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: SPIKE runner — yahoo vs Stooq on a known split date</name>
  <read_first>
    - .planning/phases/02-market-data/02-CONTEXT.md §Amendment A (D-08 candidates a/b/c)
    - .planning/phases/02-market-data/02-RESEARCH.md §Pattern 2 (chart() usage), §Pattern 3 (Stooq CSV), §Pitfall 2
    - .planning/research/PITFALLS.md Pitfall 2 (adjusted vs unadjusted)
    - db/schema.ts (existing priceSnapshots shape)
  </read_first>
  <files>scripts/spike-raw-close.ts</files>
  <action>
    Install runtime deps if missing: `pnpm add yahoo-finance2@^3.14`. Create `scripts/spike-raw-close.ts` that:
    1. Imports `yahooFinance from 'yahoo-finance2'` (default export).
    2. Defines `SYMBOL = 'AAPL'` and date window `period1 = '2020-08-20'`, `period2 = '2020-09-10'` (covers AAPL 4:1 split on 2020-08-31).
    3. Calls `await yahooFinance.chart(SYMBOL, { period1, period2, interval: '1d' })` and extracts `result.quotes` (array of `{date, open, high, low, close, adjclose, volume}`).
    4. Fetches Stooq CSV via native `fetch('https://stooq.com/q/d/l/?s=aapl.us&i=d&d1=20200820&d2=20200910')`; if response `content-type` is not `text/csv` or body starts with `<` throw an explicit error (Pitfall 4 guard).
    5. Parses Stooq CSV with `line.split(',')` per row (header: `Date,Open,High,Low,Close,Volume`).
    6. For each overlapping date, prints a row `{date, yahoo_close, yahoo_adjclose, stooq_close, diff_pct}` and computes maximum `abs(yahoo_close - stooq_close) / stooq_close * 100` across the window.
    7. Writes raw yahoo response to `lib/__tests__/fixtures/market/yahoo-chart-aapl.json` and raw Stooq CSV body to `lib/__tests__/fixtures/market/stooq-aapl-us.csv` as a side effect (so fixture capture is free).
    8. Exits 0 on success, 1 if either source fails.

    Run it: `pnpm tsx scripts/spike-raw-close.ts` (tsx is already installed from Phase 1).

    File must include `import 'server-only'` is NOT required (it is a CLI script, not a lib/ module), but top comment must state "CLI script — run with pnpm tsx, NOT imported from app code".
  </action>
  <verify>
    <automated>pnpm tsx scripts/spike-raw-close.ts</automated>
  </verify>
  <acceptance_criteria>
    - Running `pnpm tsx scripts/spike-raw-close.ts` exits 0 and prints a table with at least 15 rows (one per trading day in the window)
    - A row for `2020-08-31` (split date) or `2020-09-01` (first post-split day) exists
    - The maximum observed `diff_pct` is printed as the last line in format `MAX_DIFF_PCT=<number>`
    - `lib/__tests__/fixtures/market/yahoo-chart-aapl.json` exists and is >= 2 KB
  </acceptance_criteria>
  <done>Spike script runs, prints comparison, captures yahoo fixture as side effect.</done>
</task>

<task type="auto">
  <name>Task 2: Write SPIKE decision doc and resolve D-08</name>
  <read_first>
    - scripts/spike-raw-close.ts (Task 1 output)
    - .planning/phases/02-market-data/02-CONTEXT.md §Amendment A D-08
    - .planning/research/PITFALLS.md Pitfall 2
  </read_first>
  <files>.planning/phases/02-market-data/02-SPIKE-RAW-CLOSE.md</files>
  <action>
    Based on the numeric output from Task 1, choose one of D-08 candidates:
    - (a) `raw_close = close` (same value, no split detection)
    - (b) `raw_close` nullable, populated only when Stooq secondary fetch succeeds

    **Scope constraint:** This SPIKE MUST select option (a) or (b).
    Option (c) "Stooq as primary raw source for US" is OUT OF SCOPE for Phase 2 —
    it would require re-planning Plan 04 (yahoo client) and Plan 08 (orchestrator)
    to add dual-source fetch. If SPIKE evidence suggests (c) is necessary, stop
    and escalate: file a deferred idea in CONTEXT.md and pick option (b) as the
    Phase 2 interim.

    Write `02-SPIKE-RAW-CLOSE.md` with sections:
    1. `## Experiment` — AAPL 2020-08-31 window, command used, date run
    2. `## Raw Results` — paste the table of `{date, yahoo_close, yahoo_adjclose, stooq_close, diff_pct}`
    3. `## Observation` — numeric facts: max diff_pct, whether yahoo `close` equals `adjclose` pre-split vs post-split, whether Stooq is split-adjusted or not
    4. `## Decision` — one of (a)/(b)/(c) chosen, with rationale tied to observed numbers
    5. `## Implications for Wave 1+` — update rules for every fetcher: (i) what value fills `raw_close`, (ii) whether Stooq must be called on success path (not just fallback), (iii) any new test cases

    Decision rule of thumb: if yahoo `adjclose` != `close` on the split date, yahoo chart() already gives both raw and adjusted — pick (a) with `raw_close = quote.close` and existing `close = quote.adjclose`. If yahoo returns only one value, pick (b). Never pick (c) in Phase 2 (see scope constraint above).
  </action>
  <verify>
    <automated>test -s .planning/phases/02-market-data/02-SPIKE-RAW-CLOSE.md && grep -q "## Decision" .planning/phases/02-market-data/02-SPIKE-RAW-CLOSE.md && grep -qE "\\(a\\)|\\(b\\)" .planning/phases/02-market-data/02-SPIKE-RAW-CLOSE.md && ! grep -qE "chose.*\\(c\\)|selected.*\\(c\\)" .planning/phases/02-market-data/02-SPIKE-RAW-CLOSE.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists with all 5 required sections
    - `## Decision` section explicitly names (a) or (b) — never (c)
    - Numeric evidence from Task 1 output is quoted in `## Raw Results`
    - `## Implications for Wave 1+` lists at least 2 concrete rules for subsequent plans
  </acceptance_criteria>
  <done>D-08 resolved with written decision; Wave 1 unblocked.</done>
</task>

<task type="auto">
  <name>Task 3: Capture remaining offline fixtures (Finnhub + Stooq JP + Stooq error HTML + yahoo JP)</name>
  <read_first>
    - scripts/spike-raw-close.ts (pattern to reuse)
    - lib/env.ts (FINNHUB_API_KEY not yet added — use process.env.FINNHUB_API_KEY ?? exit with TODO)
    - .planning/phases/02-market-data/02-RESEARCH.md §Stooq quirks
  </read_first>
  <files>
    lib/__tests__/fixtures/market/yahoo-chart-7203-T.json,
    lib/__tests__/fixtures/market/finnhub-news-aapl.json,
    lib/__tests__/fixtures/market/finnhub-basicfinancials-aapl.json,
    lib/__tests__/fixtures/market/stooq-7203-jp.csv,
    lib/__tests__/fixtures/market/stooq-error.html,
    scripts/capture-fixtures.ts
  </files>
  <action>
    Create `scripts/capture-fixtures.ts` that writes 5 fixture files. Run it once via `pnpm tsx scripts/capture-fixtures.ts`. It must:

    1. **yahoo JP** — `await yahooFinance.chart('7203.T', { period1: '2026-01-01', period2: '2026-04-10', interval: '1d' })` → write `lib/__tests__/fixtures/market/yahoo-chart-7203-T.json` via `JSON.stringify(result, null, 2)`.

    2. **Finnhub news** — `fetch('https://finnhub.io/api/v1/company-news?symbol=AAPL&from=2026-04-01&to=2026-04-10&token=' + process.env.FINNHUB_API_KEY)`. If `FINNHUB_API_KEY` is missing, write a hand-crafted minimal fixture instead: `[{ "category": "company", "datetime": 1712000000, "headline": "Apple fixture headline", "id": 1, "image": "", "related": "AAPL", "source": "Fixture", "summary": "Short summary", "url": "https://example.com" }]`. Write to `finnhub-news-aapl.json`.

    3. **Finnhub basic financials** — `fetch('https://finnhub.io/api/v1/stock/metric?symbol=AAPL&metric=all&token=...')`. Same fallback: if no key, write `{ "metric": { "peBasicExclExtraTTM": 28.5, "epsBasicExclExtraItemsTTM": 6.2, "marketCapitalization": 2800000, "52WeekHigh": 200.0, "52WeekLow": 150.0 }, "series": {}, "symbol": "AAPL" }`. Write to `finnhub-basicfinancials-aapl.json`.

    4. **Stooq JP CSV** — `fetch('https://stooq.com/q/d/l/?s=7203.jp&i=d&d1=20260101&d2=20260410')`, verify `content-type` starts with `text/csv` AND first line equals `Date,Open,High,Low,Close,Volume`; write body to `stooq-7203-jp.csv`. If content-type is HTML (Stooq outage), write a known-good hand-crafted CSV header + 5 rows with plausible Toyota prices (2500-2700 JPY range).

    5. **Stooq error HTML** — hand-craft a minimal HTML error response: `<html><head><title>Stooq</title></head><body>No data</body></html>` → write to `stooq-error.html`. (This is a synthetic fixture; no network call.)

    Every fixture must be committable (do NOT embed API keys or PII).
  </action>
  <verify>
    <automated>pnpm tsx scripts/capture-fixtures.ts && test -s lib/__tests__/fixtures/market/yahoo-chart-7203-T.json && test -s lib/__tests__/fixtures/market/finnhub-news-aapl.json && test -s lib/__tests__/fixtures/market/finnhub-basicfinancials-aapl.json && test -s lib/__tests__/fixtures/market/stooq-7203-jp.csv && test -s lib/__tests__/fixtures/market/stooq-error.html</automated>
  </verify>
  <acceptance_criteria>
    - All 5 fixture files exist and are non-empty
    - `stooq-7203-jp.csv` first line equals `Date,Open,High,Low,Close,Volume`
    - `stooq-error.html` starts with `<html` (used as content-type-check negative sample)
    - No fixture contains the literal string of the FINNHUB_API_KEY env var value
  </acceptance_criteria>
  <done>All Wave 0 fixture requirements from 02-VALIDATION.md §Wave 0 Requirements are satisfied.</done>
</task>

</tasks>

<verification>
  - `pnpm tsx scripts/spike-raw-close.ts` exits 0
  - `.planning/phases/02-market-data/02-SPIKE-RAW-CLOSE.md` exists and names a D-08 decision
  - 6 fixture files exist under `lib/__tests__/fixtures/market/`
  - Wave 1 can start; no Wave 1 task references `raw_close` logic without citing this plan's decision
</verification>

<success_criteria>
  - D-08 amendment closed with written decision
  - Offline fixtures ready for Wave 1+ unit tests
  - Wave 0 block lifted
</success_criteria>

<output>
After completion, create `.planning/phases/02-market-data/02-00-SUMMARY.md`
</output>
</content>
</invoke>