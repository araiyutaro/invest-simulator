---
phase: 04-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - app/layout.tsx
  - app/dashboard/page.tsx
  - app/dashboard/components/DashboardHeader.tsx
  - config/tickers.ts
  - package.json
autonomous: true
requirements:
  - DASH-01
  - DASH-02

must_haves:
  truths:
    - "ダッシュボードページがダークテーマ（slate-900背景）で表示される"
    - "ヘッダーにアプリ名とサインアウトボタンが表示される"
    - "1306.T がティッカーホワイトリストに存在する"
    - "lightweight-charts, recharts がインストール済み"
  artifacts:
    - path: "app/globals.css"
      provides: "ダークテーマ配色 + Tailwind v4 @theme 設定"
      contains: "--color-background: #0f172a"
    - path: "app/dashboard/components/DashboardHeader.tsx"
      provides: "ヘッダーコンポーネント"
      contains: "AI投資観察ダッシュボード"
    - path: "app/dashboard/page.tsx"
      provides: "ダッシュボードページシェル（セクション配置順）"
      contains: "DashboardHeader"
    - path: "config/tickers.ts"
      provides: "1306.T ホワイトリストエントリ"
      contains: "1306.T"
  key_links:
    - from: "app/dashboard/page.tsx"
      to: "app/dashboard/components/DashboardHeader.tsx"
      via: "import"
      pattern: "import.*DashboardHeader"
---

<objective>
ダッシュボードの基盤セットアップ: ダークテーマ配色、チャートライブラリのインストール、1306.T ホワイトリスト追加、ページシェル（セクション配置）を確立する。

Purpose: 後続の全コンポーネント（Plan 02-04）がこの基盤の上に構築される。ダークテーマとライブラリが先に確定することで、各コンポーネントが独立して開発可能になる。
Output: ダークテーマのページシェル + インストール済みライブラリ + 1306.T ホワイトリスト
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dashboard/04-CONTEXT.md
@.planning/phases/04-dashboard/04-UI-SPEC.md
@.planning/phases/04-dashboard/04-RESEARCH.md

@app/globals.css
@app/layout.tsx
@app/dashboard/page.tsx
@config/tickers.ts
@lib/market/types.ts
@lib/session.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: ダークテーマ globals.css + layout.tsx 更新 + DashboardHeader</name>
  <files>app/globals.css, app/layout.tsx, app/dashboard/components/DashboardHeader.tsx</files>
  <read_first>
    - app/globals.css (現在のTailwind設定を確認)
    - app/layout.tsx (Geistフォント設定、html/bodyクラスを確認)
    - .planning/phases/04-dashboard/04-UI-SPEC.md (配色・タイポグラフィ仕様)
  </read_first>
  <action>
1. `app/globals.css` を以下に書き換える（per D-03: ダークテーマをベース配色で設計、`dark:` クラスは使わない）:

```css
@import "tailwindcss";

@theme inline {
  --color-background: #0f172a;
  --color-foreground: #f1f5f9;
  --color-surface: #1e293b;
  --color-surface-elevated: #334155;
  --color-border: #334155;
  --color-text-primary: #f1f5f9;
  --color-text-muted: #94a3b8;
  --color-text-neutral: #cbd5e1;
  --color-accent-blue: #60a5fa;
  --color-accent-green: #4ade80;
  --color-accent-red: #f87171;
  --color-accent-amber: #fbbf24;
  --font-sans: var(--font-geist-sans);
  --font-mono: var(--font-geist-mono);
}

body {
  background-color: var(--color-background);
  color: var(--color-foreground);
}
```

注意: `:root` のライトテーマ変数と `@media (prefers-color-scheme: dark)` ブロックを完全に削除する。`font-family: Arial` も削除（Geist Sans を `--font-sans` で使用するため）。

2. `app/layout.tsx` の `<html>` タグの `lang` 属性を `"ja"` に変更。`<body>` の className に `font-sans` を追加（Tailwind v4 の `--font-sans` でGeistが適用される）。`metadata` の `title` を `"AI投資観察ダッシュボード"` に、`description` を `"AIエージェントの売買判断と思考プロセスを追跡するダッシュボード"` に変更。

3. `app/dashboard/components/DashboardHeader.tsx` を新規作成（Server Component）:

```tsx
import { sessionOptions } from '@/lib/session'

export function DashboardHeader() {
  return (
    <header className="flex justify-between items-center h-14 px-8 bg-slate-900 border-b border-slate-800">
      <h1 className="text-lg font-semibold text-slate-100">AI投資観察ダッシュボード</h1>
      <form action="/api/auth/logout" method="POST">
        <button
          type="submit"
          className="text-sm text-slate-400 hover:text-slate-100 transition-colors"
        >
          サインアウト
        </button>
      </form>
    </header>
  )
}
```
  </action>
  <verify>
    <automated>cd /Users/arai/WebstormProjects/invest-simulator && grep -q "#0f172a" app/globals.css && grep -q "AI投資観察ダッシュボード" app/dashboard/components/DashboardHeader.tsx && grep -q 'lang="ja"' app/layout.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - app/globals.css contains `--color-background: #0f172a`
    - app/globals.css contains `--color-surface: #1e293b`
    - app/globals.css does NOT contain `prefers-color-scheme`
    - app/globals.css does NOT contain `font-family: Arial`
    - app/layout.tsx contains `lang="ja"`
    - app/layout.tsx contains `AI投資観察ダッシュボード` in metadata title
    - app/layout.tsx body contains `font-sans` class
    - app/dashboard/components/DashboardHeader.tsx contains `AI投資観察ダッシュボード`
    - app/dashboard/components/DashboardHeader.tsx contains `サインアウト`
    - app/dashboard/components/DashboardHeader.tsx contains `h-14 px-8 bg-slate-900 border-b border-slate-800`
  </acceptance_criteria>
  <done>globals.css がダークテーマ配色に更新され、DashboardHeader が UI-SPEC 通りのスタイルで作成されている</done>
</task>

<task type="auto">
  <name>Task 2: チャートライブラリインストール + 1306.T ホワイトリスト追加 + ページシェル</name>
  <files>package.json, config/tickers.ts, app/dashboard/page.tsx</files>
  <read_first>
    - config/tickers.ts (現在のティッカー一覧)
    - app/dashboard/page.tsx (現在のplaceholder)
    - .planning/phases/04-dashboard/04-CONTEXT.md (D-07, D-10)
    - .planning/phases/04-dashboard/04-RESEARCH.md (インストールバージョン)
  </read_first>
  <action>
1. チャートライブラリをインストール（per D-10）:
```bash
npm install lightweight-charts lightweight-charts-react-wrapper recharts
```
RESEARCH.md で確認済みバージョン: lightweight-charts@5.1.0, lightweight-charts-react-wrapper@2.1.1, recharts@3.8.1

2. `config/tickers.ts` に 1306.T を追加（per D-07, CONTEXT.md specifics）。SPY の行の直後に追加:
```typescript
{ symbol: '1306.T', market: 'JP', name: 'TOPIX連動型上場投信', currency: 'JPY', assetClass: 'etf' },
```
これによりPhase 2のデータ取得パイプラインが自動的に1306.Tの価格をprice_snapshotsに蓄積する。

3. `app/dashboard/page.tsx` をページシェルに書き換える（per D-01 1ページスクロール、D-02 セクション配置順）:

```tsx
import { DashboardHeader } from './components/DashboardHeader'

export default async function DashboardPage() {
  return (
    <>
      <DashboardHeader />
      <main className="max-w-7xl mx-auto px-8 py-12 space-y-12">
        {/* Section 1: パフォーマンス指標カード (DASH-04) */}
        <section aria-label="パフォーマンス指標">
          <h2 className="text-xl font-semibold text-slate-100 mb-6">パフォーマンス</h2>
          <p className="text-sm text-slate-400">指標データは Plan 03 で実装</p>
        </section>

        {/* Section 2: ポートフォリオ推移チャート (DASH-01) */}
        <section aria-label="ポートフォリオ推移">
          <h2 className="text-xl font-semibold text-slate-100 mb-6">ポートフォリオ推移</h2>
          <p className="text-sm text-slate-400">チャートは Plan 03 で実装</p>
        </section>

        {/* Section 3: ポジション一覧 + 配分パイチャート (DASH-02) */}
        <section aria-label="ポジション">
          <h2 className="text-xl font-semibold text-slate-100 mb-6">ポジション</h2>
          <p className="text-sm text-slate-400">ポジションテーブルは Plan 03 で実装</p>
        </section>

        {/* Section 4: トレードタイムライン (DASH-03) */}
        <section aria-label="トレードタイムライン">
          <h2 className="text-xl font-semibold text-slate-100 mb-6">トレードタイムライン</h2>
          <p className="text-sm text-slate-400">タイムラインは Plan 04 で実装</p>
        </section>
      </main>
    </>
  )
}
```

注意: 各セクションはplaceholderテキストのみ。実コンポーネントは Plan 03, 04 で差し替える。`async` キーワードは Plan 03 での DB クエリ追加に備えて付与。
  </action>
  <verify>
    <automated>cd /Users/arai/WebstormProjects/invest-simulator && grep -q "1306.T" config/tickers.ts && grep -q "DashboardHeader" app/dashboard/page.tsx && node -e "const pkg = require('./package.json'); const deps = pkg.dependencies || {}; if (!deps['lightweight-charts'] || !deps['recharts']) { process.exit(1); }" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - config/tickers.ts contains `{ symbol: '1306.T', market: 'JP', name: 'TOPIX連動型上場投信', currency: 'JPY', assetClass: 'etf' }`
    - package.json contains `lightweight-charts` in dependencies
    - package.json contains `lightweight-charts-react-wrapper` in dependencies
    - package.json contains `recharts` in dependencies
    - app/dashboard/page.tsx imports DashboardHeader
    - app/dashboard/page.tsx contains `max-w-7xl mx-auto px-8 py-12 space-y-12`
    - app/dashboard/page.tsx contains 4 `<section>` elements in order: パフォーマンス指標, ポートフォリオ推移, ポジション, トレードタイムライン
    - `npx tsc --noEmit` exits 0 (TypeScript compilation passes)
  </acceptance_criteria>
  <done>チャートライブラリがインストールされ、1306.T がホワイトリストに追加され、ページシェルが D-01/D-02 の配置順で構築されている</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → server | ページシェルのみ。ユーザー入力なし |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Spoofing | DashboardHeader Sign out form | accept | Sign out は既存の /api/auth/logout POST endpoint を使用。proxy.ts で /dashboard は認証保護済み |
</threat_model>

<verification>
- `app/globals.css` がダーク配色に更新されている
- `app/dashboard/page.tsx` が4セクション構造のシェルになっている
- `config/tickers.ts` に 1306.T エントリが存在する
- `lightweight-charts`, `lightweight-charts-react-wrapper`, `recharts` が package.json に存在する
- TypeScript コンパイルが通る
</verification>

<success_criteria>
ダッシュボードページにアクセスすると、slate-900 背景のダークテーマで、ヘッダー（アプリ名 + サインアウト）と4つのセクションplaceholderが表示される
</success_criteria>

<output>
After completion, create `.planning/phases/04-dashboard/04-01-SUMMARY.md`
</output>
