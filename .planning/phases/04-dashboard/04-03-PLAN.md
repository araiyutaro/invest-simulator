---
phase: 04-dashboard
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified:
  - app/dashboard/page.tsx
  - app/dashboard/components/PerformanceGrid.tsx
  - app/dashboard/components/PortfolioChart.tsx
  - app/dashboard/components/PositionsTable.tsx
  - app/dashboard/components/AllocationChart.tsx
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-04

must_haves:
  truths:
    - "パフォーマンス指標が3x2グリッドで表示され、プラスは緑・マイナスは赤で色分けされる"
    - "ポートフォリオ推移チャートに3系列（ポートフォリオ・SPY・TOPIX）が%正規化で表示される"
    - "ポジション一覧テーブルに保有数・取得価格・現在価格・含み損益・配分比率が表示される"
    - "配分比率がドーナツチャートで可視化される"
    - "データ未蓄積時にempty stateメッセージが表示される"
  artifacts:
    - path: "app/dashboard/components/PerformanceGrid.tsx"
      provides: "6指標の3x2グリッドカード"
      contains: "grid grid-cols-3"
    - path: "app/dashboard/components/PortfolioChart.tsx"
      provides: "lightweight-charts ラインチャート"
      contains: "use client"
    - path: "app/dashboard/components/PositionsTable.tsx"
      provides: "ポジション一覧テーブル"
      contains: "含み損益"
    - path: "app/dashboard/components/AllocationChart.tsx"
      provides: "Recharts ドーナツチャート"
      contains: "use client"
    - path: "app/dashboard/page.tsx"
      provides: "全コンポーネント統合ページ"
      contains: "PerformanceGrid"
  key_links:
    - from: "app/dashboard/page.tsx"
      to: "lib/dashboard/queries.ts"
      via: "import getChartData, getPositionsWithPrices, getPerformanceData"
      pattern: "import.*from.*@/lib/dashboard/queries"
    - from: "app/dashboard/page.tsx"
      to: "lib/dashboard/metrics.ts"
      via: "import calculateMetrics, normalizeToPercent"
      pattern: "import.*from.*@/lib/dashboard/metrics"
    - from: "app/dashboard/page.tsx"
      to: "app/dashboard/components/PortfolioChart.tsx"
      via: "dynamic import with ssr: false"
      pattern: "dynamic.*PortfolioChart.*ssr.*false"
---

<objective>
パフォーマンス指標グリッド、ポートフォリオ推移チャート、ポジションテーブル、配分パイチャートの4コンポーネントを実装し、page.tsx に統合する。

Purpose: ダッシュボードの上3セクション（指標カード・推移チャート・ポジション＋配分）を完成させ、Core Valueの「運用成果を一覧で把握する」体験を提供する。
Output: 4コンポーネント + page.tsx 統合
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-dashboard/04-CONTEXT.md
@.planning/phases/04-dashboard/04-UI-SPEC.md
@.planning/phases/04-dashboard/04-RESEARCH.md
@.planning/phases/04-dashboard/04-02-SUMMARY.md

@db/schema.ts
@lib/dashboard/types.ts
@lib/dashboard/metrics.ts
@lib/dashboard/queries.ts
@app/dashboard/page.tsx
@app/globals.css

<interfaces>
<!-- From lib/dashboard/types.ts (Plan 02 で作成) -->
```typescript
export type ChartPoint = { readonly time: string; readonly value: number }

export type PerformanceMetrics = {
  readonly totalReturn: number
  readonly spyDiff: number
  readonly sharpe: number
  readonly maxDrawdown: number
  readonly winRate: number | null
  readonly tradeCount: number
}

export type PositionWithPrice = {
  readonly symbol: string
  readonly quantity: number
  readonly avgCost: number
  readonly currentPrice: number
  readonly currency: string
  readonly unrealizedPnl: number
  readonly allocation: number
}

export type AllocationSlice = {
  readonly name: string
  readonly value: number
}
```

<!-- From lib/dashboard/metrics.ts (Plan 02 で作成) -->
```typescript
export function normalizeToPercent(series: {date: string; value: number}[]): ChartPoint[]
export function calculateMetrics(params: { snapshots: ...; spySnapshots: ...; trades: ...; positionAvgCosts: ... }): PerformanceMetrics | null
```

<!-- From lib/dashboard/queries.ts (Plan 02 で作成) -->
```typescript
export async function getPortfolioId(): Promise<string>
export async function getChartData(portfolioId: string): Promise<{ portfolio: ...; spy: ...; topix: ... }>
export async function getPositionsWithPrices(portfolioId: string): Promise<{ positions: PositionWithPrice[]; allocations: AllocationSlice[]; cash: number }>
export async function getPerformanceData(portfolioId: string): Promise<{ snapshots: ...; spySnapshots: ...; trades: ...; positionAvgCosts: ... }>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PerformanceGrid + PortfolioChart コンポーネント</name>
  <files>app/dashboard/components/PerformanceGrid.tsx, app/dashboard/components/PortfolioChart.tsx</files>
  <read_first>
    - lib/dashboard/types.ts (PerformanceMetrics, ChartPoint 型)
    - .planning/phases/04-dashboard/04-UI-SPEC.md (PerformanceGrid セクション — カード構造、色ルール、empty state; PortfolioChart セクション — チャートオプション、凡例)
    - .planning/phases/04-dashboard/04-RESEARCH.md (Pattern 2 lightweight-charts コード例、Pattern 5 メトリクス表示)
    - app/globals.css (カスタムカラー変数名の確認)
  </read_first>
  <action>
1. `app/dashboard/components/PerformanceGrid.tsx` を新規作成（**Server Component** — per D-16）:

Props: `{ metrics: PerformanceMetrics | null }`

6指標を3x2グリッドで表示（per D-17）。配置:
- [0,0] 累計リターン(%) — ラベル「累計リターン」、フォーマット `+12.34%` / `-5.67%`
- [0,1] vs SPY差分(%) — ラベル「vs SPY」、フォーマット `+2.10%`
- [0,2] シャープレシオ — ラベル「シャープレシオ」、フォーマット `1.23`（小数2桁）
- [1,0] 最大ドローダウン(%) — ラベル「最大DD」、フォーマット `-8.45%`（常にマイナス表示）
- [1,1] 勝率(%) — ラベル「勝率」、フォーマット `65.0%`
- [1,2] 取引数 — ラベル「取引数」、フォーマット整数

カード構造（UI-SPEC 準拠）:
```tsx
<div className="bg-slate-800 rounded-lg p-6">
  <p className="text-xs text-slate-400 mb-2">{ラベル}</p>
  <p className={`text-3xl font-semibold font-mono ${色クラス}`}>{値}</p>
</div>
```

色ルール:
- 累計リターン/vs SPY/シャープレシオ: >=0 → `text-green-400`, <0 → `text-red-400`
- 最大DD: 常に `text-red-400`
- 勝率: >=50 → `text-green-400`, <50 → `text-red-400`
- 取引数: `text-slate-300`

Empty state (`metrics === null`): 全カードに `—` を `text-slate-400` で表示。

グリッドクラス: `grid grid-cols-3 gap-4`（レスポンシブ: `grid-cols-1 md:grid-cols-3`）

2. `app/dashboard/components/PortfolioChart.tsx` を新規作成（**Client Component** `"use client"` — per D-05）:

Props: `{ portfolio: ChartPoint[]; spy: ChartPoint[]; topix: ChartPoint[] }`

lightweight-charts-react-wrapper を使用:
```tsx
'use client'
import { Chart, LineSeries } from 'lightweight-charts-react-wrapper'
import type { ChartPoint } from '@/lib/dashboard/types'
```

チャートオプション（UI-SPEC 準拠）:
- `layout.background.color: '#0f172a'` (slate-900)
- `layout.textColor: '#cbd5e1'` (slate-300)
- `grid.vertLines.color: '#1e293b'` (slate-800)
- `grid.horzLines.color: '#1e293b'` (slate-800)
- `height: 400` (固定)
- `autoSize: true` (幅は親に追従)

3系列:
- ポートフォリオ: `color='#60a5fa'` (blue-400), `lineWidth={2}`
- SPY: `color='#64748b'` (slate-500), `lineWidth={1}`
- TOPIX: `color='#475569'` (slate-600), `lineWidth={1}`

凡例: チャート上部に CSS overlay で `ポートフォリオ / SPY / 1306.T` をそれぞれの色で表示:
```tsx
<div className="relative">
  <div className="absolute top-2 right-4 flex gap-4 text-xs z-10">
    <span className="text-blue-400">● ポートフォリオ</span>
    <span className="text-slate-500">● SPY</span>
    <span style={{ color: '#475569' }}>● 1306.T</span>
  </div>
  <div aria-label="ポートフォリオ推移チャート">
    <Chart ...>
      <LineSeries ... />
    </Chart>
  </div>
</div>
```

Empty state: `portfolio.length === 0` の場合、チャートの代わりに:
```tsx
<div className="h-[400px] bg-slate-800 rounded-lg flex items-center justify-center">
  <p className="text-sm text-slate-400">運用開始後にデータが表示されます</p>
</div>
```
  </action>
  <verify>
    <automated>cd /Users/arai/WebstormProjects/invest-simulator && grep -q "grid grid-cols" app/dashboard/components/PerformanceGrid.tsx && grep -q "use client" app/dashboard/components/PortfolioChart.tsx && grep -q "LineSeries" app/dashboard/components/PortfolioChart.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - PerformanceGrid.tsx does NOT contain `"use client"` (Server Component)
    - PerformanceGrid.tsx contains `grid grid-cols-1 md:grid-cols-3 gap-4` or equivalent
    - PerformanceGrid.tsx contains 6 metric cards: 累計リターン, vs SPY, シャープレシオ, 最大DD, 勝率, 取引数
    - PerformanceGrid.tsx contains `text-3xl font-semibold font-mono` for metric values
    - PerformanceGrid.tsx handles null metrics with `—` display
    - PortfolioChart.tsx first line is `'use client'`
    - PortfolioChart.tsx imports from `lightweight-charts-react-wrapper`
    - PortfolioChart.tsx renders 3 LineSeries with colors #60a5fa, #64748b, #475569
    - PortfolioChart.tsx has empty state with text `運用開始後にデータが表示されます`
    - PortfolioChart.tsx contains `aria-label="ポートフォリオ推移チャート"`
  </acceptance_criteria>
  <done>PerformanceGrid が6指標の3x2グリッドを色分け表示し、PortfolioChart が3系列のラインチャートを描画する</done>
</task>

<task type="auto">
  <name>Task 2: PositionsTable + AllocationChart コンポーネント</name>
  <files>app/dashboard/components/PositionsTable.tsx, app/dashboard/components/AllocationChart.tsx</files>
  <read_first>
    - lib/dashboard/types.ts (PositionWithPrice, AllocationSlice 型)
    - .planning/phases/04-dashboard/04-UI-SPEC.md (PositionsTable セクション — カラム定義、CASH行、empty state; AllocationChart セクション — ドーナツ仕様、色パレット)
    - .planning/phases/04-dashboard/04-RESEARCH.md (Pattern 3 Recharts DonutChart コード例)
  </read_first>
  <action>
1. `app/dashboard/components/PositionsTable.tsx` を新規作成（**Server Component**）:

Props: `{ positions: PositionWithPrice[]; cash: number; totalValue: number }`

テーブルカラム（UI-SPEC 準拠）:
| Column | Header | Align | Format |
|--------|--------|-------|--------|
| symbol | 銘柄 | left | `font-mono text-slate-100` |
| quantity | 保有数 | right | 整数 `font-mono` |
| avgCost | 取得平均価格 | right | `¥{N,NNN}` 円 `font-mono` |
| currentPrice | 現在価格 | right | `¥{N,NNN}` 円 `font-mono` |
| unrealizedPnl | 含み損益 | right | `±¥{N,NNN}` green-400/red-400 `font-mono` |
| allocation | 配分比率 | right | `{N.N}%` slate-300 |

スタイリング:
- テーブル: `w-full text-sm`
- ヘッダー行: `text-xs text-slate-400`
- データ行: `border-b border-slate-700`
- ソートなし（Claude's Discretion で決定済み）

現金行: 最終行に `CASH / — / — / ¥{cash.toLocaleString()} / — / {cashAllocation}%` を表示。`cashAllocation = (cash / totalValue) * 100`

数値フォーマット: `¥` + `toLocaleString('ja-JP')` でカンマ区切り。含み損益は正なら `+¥xxx` + `text-green-400`、負なら `-¥xxx` + `text-red-400`。

Empty state: `positions.length === 0` の場合:
```tsx
<p className="text-sm text-slate-400">ポジションなし — 最初の取引が実行されると表示されます</p>
```

2. `app/dashboard/components/AllocationChart.tsx` を新規作成（**Client Component** `"use client"` — per D-09）:

Props: `{ data: AllocationSlice[] }`

Recharts PieChart ドーナツ形式:
```tsx
'use client'
import { PieChart, Pie, Cell, Tooltip, ResponsiveContainer } from 'recharts'
```

色パレット（UI-SPEC 準拠、10色循環）:
```typescript
const COLORS = ['#60a5fa','#34d399','#fbbf24','#f87171','#a78bfa','#fb923c','#38bdf8','#86efac','#fde68a','#64748b']
```
最終セグメント（CASH）は常に `#64748b` (slate-500)。実装: CASH エントリが data の最後の要素であることを前提に、最後のインデックスは固定色。

ドーナツ設定: `innerRadius={60}` `outerRadius={100}`
サイズ: `ResponsiveContainer` でラップ、`width="100%"` `height={300}`
ツールチップ: `{name}: {N.N}%` 表示（value を total で割って%計算）

Empty state: `data.length === 0` の場合:
```tsx
<div className="h-[300px] flex items-center justify-center">
  <p className="text-sm text-slate-400">配分データなし</p>
</div>
```
  </action>
  <verify>
    <automated>cd /Users/arai/WebstormProjects/invest-simulator && grep -q "含み損益" app/dashboard/components/PositionsTable.tsx && grep -q "use client" app/dashboard/components/AllocationChart.tsx && grep -q "PieChart" app/dashboard/components/AllocationChart.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - PositionsTable.tsx does NOT contain `"use client"` (Server Component)
    - PositionsTable.tsx contains table headers: 銘柄, 保有数, 取得平均価格, 現在価格, 含み損益, 配分比率
    - PositionsTable.tsx contains CASH row rendering
    - PositionsTable.tsx uses `font-mono` for all numeric cells
    - PositionsTable.tsx contains empty state text `ポジションなし`
    - PositionsTable.tsx uses `text-green-400` for positive PnL and `text-red-400` for negative
    - AllocationChart.tsx first line is `'use client'`
    - AllocationChart.tsx imports PieChart, Pie, Cell, Tooltip, ResponsiveContainer from recharts
    - AllocationChart.tsx contains COLORS array with 10 hex values starting with `#60a5fa`
    - AllocationChart.tsx contains `innerRadius={60}` and `outerRadius={100}`
    - AllocationChart.tsx contains empty state text `配分データなし`
  </acceptance_criteria>
  <done>PositionsTable が6カラムのポジション一覧を CASH 行込みで表示し、AllocationChart がドーナツチャートで配分を可視化する</done>
</task>

<task type="auto">
  <name>Task 3: page.tsx 統合 — 全コンポーネント接続 + DBクエリ</name>
  <files>app/dashboard/page.tsx</files>
  <read_first>
    - app/dashboard/page.tsx (Plan 01 で作成したシェル)
    - lib/dashboard/queries.ts (getPortfolioId, getChartData, getPositionsWithPrices, getPerformanceData)
    - lib/dashboard/metrics.ts (calculateMetrics, normalizeToPercent)
    - lib/dashboard/types.ts (型定義)
    - app/dashboard/components/PerformanceGrid.tsx (Task 1 で作成)
    - app/dashboard/components/PortfolioChart.tsx (Task 1 で作成)
    - app/dashboard/components/PositionsTable.tsx (Task 2 で作成)
    - app/dashboard/components/AllocationChart.tsx (Task 2 で作成)
    - .planning/phases/04-dashboard/04-RESEARCH.md (Pitfall 1: SSR回避 dynamic import)
  </read_first>
  <action>
`app/dashboard/page.tsx` を書き換える。Server Component として DB クエリを実行し、計算済みデータをコンポーネントに渡す。

重要ポイント:
- PortfolioChart と AllocationChart は `next/dynamic` で `{ ssr: false }` インポート（Canvas/SVG コンポーネントの SSR エラー回避 — RESEARCH.md Pitfall 1）
- PerformanceGrid と PositionsTable は通常 import（Server Component）
- DashboardHeader は通常 import（Server Component）
- DB クエリは try/catch でラップし、エラー時はエラーメッセージを表示

```tsx
import { DashboardHeader } from './components/DashboardHeader'
import { PerformanceGrid } from './components/PerformanceGrid'
import { PositionsTable } from './components/PositionsTable'
import dynamic from 'next/dynamic'
import { getPortfolioId, getChartData, getPositionsWithPrices, getPerformanceData } from '@/lib/dashboard/queries'
import { calculateMetrics, normalizeToPercent } from '@/lib/dashboard/metrics'

const PortfolioChart = dynamic(
  () => import('./components/PortfolioChart').then(mod => ({ default: mod.PortfolioChart })),
  { ssr: false }
)
const AllocationChart = dynamic(
  () => import('./components/AllocationChart').then(mod => ({ default: mod.AllocationChart })),
  { ssr: false }
)

export default async function DashboardPage() {
  let portfolioId: string
  try {
    portfolioId = await getPortfolioId()
  } catch {
    return (
      <>
        <DashboardHeader />
        <main className="max-w-7xl mx-auto px-8 py-12">
          <p className="text-sm text-slate-400">ポートフォリオが見つかりません。最初のエージェント実行後に表示されます。</p>
        </main>
      </>
    )
  }

  const [chartData, positionData, perfData] = await Promise.all([
    getChartData(portfolioId),
    getPositionsWithPrices(portfolioId),
    getPerformanceData(portfolioId),
  ])

  const metrics = calculateMetrics(perfData)
  const portfolioChart = normalizeToPercent(chartData.portfolio)
  const spyChart = normalizeToPercent(chartData.spy)
  const topixChart = normalizeToPercent(chartData.topix)
  const totalValue = positionData.positions.reduce((sum, p) => sum + p.currentPrice * p.quantity, 0) + positionData.cash

  return (
    <>
      <DashboardHeader />
      <main className="max-w-7xl mx-auto px-8 py-12 space-y-12">
        <section aria-label="パフォーマンス指標">
          <h2 className="text-xl font-semibold text-slate-100 mb-6">パフォーマンス</h2>
          <PerformanceGrid metrics={metrics} />
        </section>

        <section aria-label="ポートフォリオ推移">
          <h2 className="text-xl font-semibold text-slate-100 mb-6">ポートフォリオ推移</h2>
          <PortfolioChart portfolio={portfolioChart} spy={spyChart} topix={topixChart} />
        </section>

        <section aria-label="ポジション">
          <h2 className="text-xl font-semibold text-slate-100 mb-6">ポジション</h2>
          <div className="flex flex-col lg:flex-row gap-8">
            <div className="lg:w-3/5">
              <PositionsTable positions={positionData.positions} cash={positionData.cash} totalValue={totalValue} />
            </div>
            <div className="lg:w-2/5">
              <AllocationChart data={positionData.allocations} />
            </div>
          </div>
        </section>

        {/* Section 4: TradeTimeline — Plan 04 で実装 */}
        <section aria-label="トレードタイムライン">
          <h2 className="text-xl font-semibold text-slate-100 mb-6">トレードタイムライン</h2>
          <p className="text-sm text-slate-400">タイムラインは Plan 04 で実装</p>
        </section>
      </main>
    </>
  )
}
```

注意:
- `Promise.all` で3クエリを並列実行（パフォーマンス最適化）
- PositionsTable と AllocationChart のポジションセクションは `lg:flex-row` で横並び（UI-SPEC: 左60% + 右40%）、モバイルは縦積み
- タイムラインセクションは Plan 04 用のplaceholderを残す
  </action>
  <verify>
    <automated>cd /Users/arai/WebstormProjects/invest-simulator && grep -q "PerformanceGrid" app/dashboard/page.tsx && grep -q "PortfolioChart" app/dashboard/page.tsx && grep -q "PositionsTable" app/dashboard/page.tsx && grep -q "AllocationChart" app/dashboard/page.tsx && grep -q "ssr: false" app/dashboard/page.tsx && grep -q "Promise.all" app/dashboard/page.tsx && npx tsc --noEmit && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx imports DashboardHeader, PerformanceGrid, PositionsTable as static imports
    - page.tsx imports PortfolioChart and AllocationChart via `dynamic(() => ..., { ssr: false })`
    - page.tsx imports from `@/lib/dashboard/queries` and `@/lib/dashboard/metrics`
    - page.tsx uses `Promise.all` to parallelize getChartData, getPositionsWithPrices, getPerformanceData
    - page.tsx calls `normalizeToPercent` for portfolio, spy, topix data
    - page.tsx calls `calculateMetrics` with perfData
    - page.tsx contains `lg:w-3/5` and `lg:w-2/5` for positions layout
    - page.tsx contains error fallback when getPortfolioId fails
    - page.tsx contains 4 `<section>` elements with aria-label attributes
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>page.tsx が全コンポーネントを統合し、Server Component として DB クエリ→計算→props 渡しの完全なデータフローが構築されている</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB → Server Component | Drizzle パラメタライズドクエリ。server-only ガード済み |
| Server → Client Component | シリアライズ可能な数値・文字列のみをpropsで渡す |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Information Disclosure | page.tsx | mitigate | DB クレデンシャルは Server Component 内のみ。Client Component には計算済み数値のみ渡す。`db/index.ts` の `server-only` ガードがビルド時に検証 |
| T-04-05 | Denial of Service | page.tsx Promise.all | accept | 3クエリ並列は個人プロジェクトで許容。レート制限不要 |
</threat_model>

<verification>
- TypeScript コンパイル (`npx tsc --noEmit`) が通る
- 全コンポーネントが UI-SPEC の仕様通りのクラス名・構造を持つ
- PortfolioChart と AllocationChart が `ssr: false` で dynamic import されている
- Empty state が全コンポーネントで実装されている
</verification>

<success_criteria>
ダッシュボードページにアクセスすると、パフォーマンス指標グリッド・推移チャート・ポジションテーブル・配分パイチャートが表示される（データがない場合は empty state メッセージ）
</success_criteria>

<output>
After completion, create `.planning/phases/04-dashboard/04-03-SUMMARY.md`
</output>
