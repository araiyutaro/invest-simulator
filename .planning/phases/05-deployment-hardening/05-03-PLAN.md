---
phase: 05
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - proxy.ts
  - .env.example
autonomous: true
requirements: [OPS-03]
must_haves:
  truths:
    - "proxy.ts matcher が _next/static, _next/image, favicon.ico, sitemap.xml, robots.txt を除外し、それ以外は全て認証ゲートを通る"
    - "proxy.ts の `/login`, `/api/auth/*`, `/api/cron/*` 関数内 bypass が維持されている (Phase 01 D-14 を壊さない)"
    - ".env.example に lib/env.ts の zod schema が検証する全変数 (DATABASE_URL, GEMINI_API_KEY, SESSION_SECRET, SITE_PASSWORD, CRON_SECRET, FINNHUB_API_KEY) が記載されている"
    - ".env.example のコメントに SESSION_SECRET/CRON_SECRET の生成方法と Vercel Dashboard で手動登録する旨が明記されている"
    - "DATABASE_URL_DIRECT は 'local migrate 用' コメント付きで残る (Production 不要)"
  artifacts:
    - path: "proxy.ts"
      provides: "Updated matcher excluding sitemap.xml + robots.txt"
      contains: "sitemap.xml"
    - path: ".env.example"
      provides: "Documented env var inventory for Vercel Dashboard manual registration"
      contains: "DATABASE_URL"
  key_links:
    - from: "proxy.ts config.matcher"
      to: "Next.js routing layer"
      via: "Next.js proxy matcher regex"
      pattern: "_next/static\\|_next/image\\|favicon.ico\\|sitemap.xml\\|robots.txt"
    - from: ".env.example"
      to: "lib/env.ts zod schema"
      via: "manual Vercel Dashboard env registration (D-08)"
      pattern: "envSchema"
---

<objective>
二つの独立した安全網を固める:

1. **proxy.ts matcher 強化** — 現状 `_next/static|_next/image|favicon.ico` のみ除外だが、Next.js 公式推奨 matcher は metadata ファイル (`sitemap.xml`, `robots.txt`) も除外する。今は未実装なので実害ゼロだが将来追加時の事故予防。
2. **.env.example 監査** — lib/env.ts が検証する全変数を網羅し、CONTEXT.md D-07 の手動 Vercel Dashboard 登録 (D-08) のドキュメント源にする。CONTEXT.md 記載の `SESSION_PASSWORD` は実コードの `SESSION_SECRET` に整合させる。

両タスクは互いに独立、単一ファイル編集のみ。

Output: proxy.ts (matcher 1 行変更) + .env.example (コメント整理と検証の網羅性確認)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-deployment-hardening/05-CONTEXT.md
@.planning/phases/05-deployment-hardening/05-RESEARCH.md
@proxy.ts
@lib/env.ts
@.env.example
@.planning/phases/01-foundation/01-CONTEXT.md

<interfaces>
<!-- From lib/env.ts zod schema (source of truth for required vars) -->
```ts
const envSchema = z.object({
  DATABASE_URL: z.string().url().min(1),
  GEMINI_API_KEY: z.string().min(1),
  SESSION_SECRET: z.string().min(32),
  SITE_PASSWORD: z.string().min(1),
  CRON_SECRET: z.string().min(1),
  FINNHUB_API_KEY: z.string().min(1),
});
```

<!-- From proxy.ts config (current matcher) -->
```ts
export const config = {
  matcher: ['/((?!_next/static|_next/image|favicon.ico).*)'],
}
```

<!-- Next.js 16 recommended matcher [CITED: node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/proxy.md] -->
```ts
matcher: ['/((?!_next/static|_next/image|favicon.ico|sitemap.xml|robots.txt).*)'],
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update proxy.ts matcher to exclude sitemap.xml + robots.txt</name>
  <files>proxy.ts</files>
  <read_first>
    - proxy.ts (全文)
    - .planning/phases/05-deployment-hardening/05-RESEARCH.md (§Pattern 6)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-14 bypass pattern)
  </read_first>
  <action>
    Edit `proxy.ts`. Change only the `matcher` line inside `export const config`:

    FROM:
    ```ts
    matcher: ['/((?!_next/static|_next/image|favicon.ico).*)'],
    ```

    TO:
    ```ts
    matcher: ['/((?!_next/static|_next/image|favicon.ico|sitemap.xml|robots.txt).*)'],
    ```

    Also update the adjacent comment on line 39-40 to:
    ```ts
    // Exclude Next.js static/image optimization assets, favicon, and metadata
    // files (sitemap.xml, robots.txt). Everything else — including /api routes —
    // flows through the auth gate. (Phase 05 Plan 03, D-14 preserved)
    ```

    Do NOT touch:
    - `import 'server-only'` / other imports
    - the `proxy` function body (Phase 01 D-14 bypass for `/login`, `/api/auth/*`, `/api/cron/*`)
    - iron-session session check logic

    Run `pnpm tsc --noEmit && pnpm next build` → must pass. Commit: `feat(05-03): widen proxy.ts matcher to exclude metadata files`.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit && pnpm next build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "sitemap.xml" proxy.ts` == 1
    - `grep -c "robots.txt" proxy.ts` == 1
    - `grep -c "_next/static" proxy.ts` == 1
    - `grep -c "_next/image" proxy.ts` == 1
    - `grep -c "favicon.ico" proxy.ts` == 1
    - `grep -c "api/cron/" proxy.ts` == 1 (function-body bypass untouched)
    - `grep -c "api/auth/" proxy.ts` == 1 (function-body bypass untouched)
    - `grep -c "pathname === '/login'" proxy.ts` == 1 (login bypass untouched)
    - `grep -c "getIronSession" proxy.ts` == 1 (auth logic intact)
    - `pnpm tsc --noEmit && pnpm next build` exits 0
  </acceptance_criteria>
  <done>Matcher updated; function body unchanged; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Audit .env.example and align with lib/env.ts</name>
  <files>.env.example</files>
  <read_first>
    - .env.example (current)
    - lib/env.ts (zod schema — source of truth)
    - .planning/phases/05-deployment-hardening/05-CONTEXT.md (D-07, D-08)
    - .planning/phases/05-deployment-hardening/05-RESEARCH.md (§Pattern 5)
  </read_first>
  <action>
    1. Run a grep audit to find ALL `process.env.*` references outside `lib/env.ts`:
       ```bash
       grep -rn "process\\.env\\." lib/ app/ db/ drizzle.config.ts 2>/dev/null | grep -v "lib/env.ts" | grep -v "\\.test\\." | grep -v "NODE_ENV"
       ```
       Review output. For any reference not already in `lib/env.ts` zod schema, note the variable name. Expected findings based on Phase 1-4 audit:
       - `DATABASE_URL_DIRECT` in `drizzle.config.ts` (migrate-only, not runtime)
       - No other missing vars (Phase 2/3 did not add Alpha Vantage)

       If the audit surfaces any unexpected env var, add it to `lib/env.ts` AND `.env.example`. If none, proceed.

    2. Replace `.env.example` with the following exact content (reorganized for clarity, same variables, richer comments aligning with D-07 / D-08 manual registration):

       ```
       # invest-simulator environment variables
       # Copy this file to .env.local for local development; fill in values.
       # NEVER prefix any key with NEXT_PUBLIC (Phase 01 D-21: all secrets are server-only).
       # Do NOT commit .env.local.
       #
       # For production (Vercel): register each variable below via the Vercel
       # Dashboard UI (Project → Settings → Environment Variables) — Phase 05 D-08.
       # Do NOT use `vercel env add` CLI.

       # -- Database -----------------------------------------------------------
       # Neon Postgres pooled connection URL (used by the app at runtime).
       # Get from: Neon Console → Connection Details → Pooled connection
       DATABASE_URL=

       # Neon Postgres direct connection URL — LOCAL MIGRATE ONLY.
       # drizzle-kit migrate requires a non-pooled connection. Not needed in
       # Vercel Production env vars (we do not run migrate in production).
       DATABASE_URL_DIRECT=

       # -- AI (Gemini) --------------------------------------------------------
       # Google Gemini API key. Get from https://aistudio.google.com/apikey
       # Free tier (1,500 req/day) covers the 1-run/day cron (Phase 01 pivot).
       GEMINI_API_KEY=

       # -- Auth (iron-session v8) --------------------------------------------
       # Cookie encryption secret. MUST be ≥32 chars (enforced by lib/env.ts zod).
       # Generate with: openssl rand -base64 48
       # Rotation: manual, annual (Phase 05 D-09).
       SESSION_SECRET=

       # Plaintext dashboard password (compared with timingSafeEqual).
       SITE_PASSWORD=

       # -- Vercel Cron -------------------------------------------------------
       # Shared secret Vercel Cron sends as `Authorization: Bearer <CRON_SECRET>`.
       # Generate with: openssl rand -hex 32
       # In Vercel Dashboard, enable this variable for PRODUCTION ONLY
       # (Pitfall 8: Preview cron invocations are not needed).
       CRON_SECRET=

       # -- Market Data (Phase 02) --------------------------------------------
       # Finnhub API key for US stock news + basic financials.
       # Free tier: https://finnhub.io/register (60 calls/min, US only)
       FINNHUB_API_KEY=
       ```

    3. Confirm every key in `.env.example` appears in `lib/env.ts` zod schema, and vice versa (except `DATABASE_URL_DIRECT` which is migrate-only and documented as such).

    Commit: `docs(05-03): audit .env.example — align with lib/env.ts + Vercel D-08 notes`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const ex=fs.readFileSync('.env.example','utf8');const src=fs.readFileSync('lib/env.ts','utf8');const required=['DATABASE_URL','GEMINI_API_KEY','SESSION_SECRET','SITE_PASSWORD','CRON_SECRET','FINNHUB_API_KEY'];for(const k of required){if(!ex.includes(k+'=')){console.error('MISSING in .env.example:',k);process.exit(1)}if(!src.includes(k+':')){console.error('MISSING in lib/env.ts:',k);process.exit(1)}}if(ex.includes('SESSION_PASSWORD')){console.error('STALE name SESSION_PASSWORD — must be SESSION_SECRET');process.exit(1)}console.log('env audit OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^DATABASE_URL=" .env.example` == 1
    - `grep -c "^DATABASE_URL_DIRECT=" .env.example` == 1
    - `grep -c "^GEMINI_API_KEY=" .env.example` == 1
    - `grep -c "^SESSION_SECRET=" .env.example` == 1
    - `grep -c "^SITE_PASSWORD=" .env.example` == 1
    - `grep -c "^CRON_SECRET=" .env.example` == 1
    - `grep -c "^FINNHUB_API_KEY=" .env.example` == 1
    - `grep -c "SESSION_PASSWORD" .env.example` == 0 (stale name absent)
    - `grep -c "NEXT_PUBLIC" .env.example` == 1 (only in the warning comment)
    - `grep -c "Vercel Dashboard" .env.example` ≥ 1 (D-08 manual registration documented)
    - `grep -c "LOCAL MIGRATE ONLY" .env.example` == 1 (DATABASE_URL_DIRECT clarified)
    - `grep -c "PRODUCTION ONLY" .env.example` == 1 (CRON_SECRET scope documented — Pitfall 8)
    - `pnpm tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>.env.example is the documented source of env vars for manual Vercel Dashboard registration; matches lib/env.ts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Next.js proxy.ts matcher → route handler | Paths excluded from matcher bypass the auth gate entirely; must be strictly limited to non-sensitive assets |
| Git repo .env.example → Vercel Dashboard env registration | Documentation boundary — accuracy of .env.example directly drives production secret completeness |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-17 | Elevation of Privilege | proxy.ts matcher over-excluding paths | mitigate | Task 1 extends matcher exclusion to only well-known public metadata files (sitemap.xml, robots.txt). All API + page routes remain inside matcher and flow through iron-session gate. Acceptance criteria grep-verifies no other path is added to the exclusion list |
| T-05-18 | Information Disclosure | Missing env var → runtime crash exposing stack trace | mitigate | Task 2 audits `.env.example` against `lib/env.ts` zod schema; zod validation in `lib/env.ts` fails fast at import time with a redacted error (no secret values printed) — verified in Phase 01 |
| T-05-03 | Information Disclosure | NEXT_PUBLIC_* prefix accidentally applied to secrets | mitigate | Task 2 `.env.example` warning line "NEVER prefix any key with NEXT_PUBLIC" is explicit; ASVS L1 §14.3.3 satisfied |
| T-05-19 | Tampering (Preview env) | CRON_SECRET leaked to Preview env leads to orphan cron invocations | mitigate | Task 2 `.env.example` documents "PRODUCTION ONLY" for CRON_SECRET (Pitfall 8); SECURITY-CHECKLIST (Plan 05-04) verifies Vercel Dashboard scope |
</threat_model>

<verification>
1. `pnpm tsc --noEmit && pnpm next build` — exits 0
2. Env audit node script in Task 2 verify — exits 0
3. proxy.ts grep assertions (Task 1 acceptance_criteria)
4. Manual spot-check: run `pnpm next build && pnpm next start` briefly, `curl -I http://localhost:3000/robots.txt` → 404 expected (not redirect to /login; matcher correctly skipped robots.txt so no auth gate triggered)
</verification>

<success_criteria>
- proxy.ts matcher matches Next.js 16 canonical recommendation
- proxy.ts function body (D-14 bypass + iron-session check) is byte-identical except for the matcher line and adjacent comment
- .env.example lists every variable in lib/env.ts zod schema (no drift)
- SESSION_PASSWORD stale name removed; SESSION_SECRET used consistently
- D-08 (manual Vercel Dashboard registration) and Pitfall 8 (CRON_SECRET production-only scope) are documented in .env.example comments
</success_criteria>

<output>
After completion, create `.planning/phases/05-deployment-hardening/05-03-SUMMARY.md` with:
- proxy.ts matcher before/after diff
- .env.example final variable inventory as a table
- Link to lib/env.ts line numbers proving alignment
</output>
</content>
</invoke>