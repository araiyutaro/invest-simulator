---
phase: 05
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vercel.json
  - app/api/cron/daily-run/route.ts
  - tests/api/cron/daily-run.test.ts
autonomous: true
requirements: [OPS-01, OPS-02]
must_haves:
  truths:
    - "vercel.json に crons 配列が存在し path=/api/cron/daily-run schedule='0 22 * * *' が宣言されている"
    - "daily-run/route.ts が GET と POST の両方で CRON_SECRET Bearer を検証し、有効なら同じハンドラを呼ぶ"
    - "GET /api/cron/daily-run with no Authorization header returns 401"
    - "GET /api/cron/daily-run with correct Bearer returns 200 (or 200 skipped for idempotent 2nd call)"
    - "maxDuration = 120 が route.ts に維持されている"
  artifacts:
    - path: "vercel.json"
      provides: "Vercel Cron schedule declaration"
      contains: "\"crons\""
    - path: "app/api/cron/daily-run/route.ts"
      provides: "GET + POST handlers, shared handleDailyRun()"
      exports: ["GET", "POST", "maxDuration"]
    - path: "tests/api/cron/daily-run.test.ts"
      provides: "Unit test for GET/POST auth guard (401 on missing/wrong Bearer)"
  key_links:
    - from: "vercel.json crons[0].path"
      to: "app/api/cron/daily-run/route.ts GET handler"
      via: "Vercel Cron runtime (GET with Authorization: Bearer $CRON_SECRET)"
      pattern: "export async function GET"
---

<objective>
Phase 5 CRITICAL GAP を閉じる。Vercel Cron は **GET** で叩くのに現状の daily-run/route.ts は POST のみ実装 → 本番デプロイしても Cron が毎日 405 を返し続け、decisions テーブルに行が1件も入らない。

本プランで:
1. `vercel.json` を新規作成し `crons` 配列で UTC 22:00 発火を宣言（D-01〜D-03）
2. `daily-run/route.ts` の既存 POST 本体を `handleDailyRun()` ヘルパに抽出し、GET と POST の両方から呼び出す
3. GET/POST 両方で CRON_SECRET Bearer を検証する挙動を vitest でロックする

**Why:** RESEARCH.md §Pitfall 1 (最大の罠) + §Pattern 1/2 に基づく必須対応。OPS-01 (cron 自動発火) と OPS-02 (Bearer 認証) を同時に成立させる。
Output: vercel.json (新規) + daily-run/route.ts (refactor) + unit test
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-deployment-hardening/05-CONTEXT.md
@.planning/phases/05-deployment-hardening/05-RESEARCH.md
@.planning/phases/05-deployment-hardening/05-VALIDATION.md
@.planning/phases/03-agent-pipeline/03-CONTEXT.md
@app/api/cron/daily-run/route.ts
@app/api/cron/fetch-market-data/route.ts
@lib/env.ts

<interfaces>
<!-- From lib/env.ts (zod-validated, server-only) -->
```ts
env.CRON_SECRET: string  // min(1)
```

<!-- From existing daily-run/route.ts (Phase 3 Plan 04) -->
```ts
export const maxDuration = 120  // MUST keep (D-12)
export async function POST(request: NextRequest): Promise<NextResponse>
export async function GET(): Promise<NextResponse>  // currently returns 405 — MUST replace
```

<!-- Vercel Cron header format [CITED: vercel.com/docs/cron-jobs/manage-cron-jobs#securing-cron-jobs] -->
`Authorization: Bearer ${CRON_SECRET}`  // method: GET, user-agent: vercel-cron/1.0
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — daily-run auth-guard unit test (RED first)</name>
  <files>tests/api/cron/daily-run.test.ts</files>
  <read_first>
    - app/api/cron/daily-run/route.ts (current POST + 405 GET)
    - app/api/cron/fetch-market-data/route.ts (auth pattern reference)
    - lib/env.ts (env.CRON_SECRET typing)
    - .planning/phases/05-deployment-hardening/05-VALIDATION.md (Wave 0 section)
  </read_first>
  <behavior>
    - Test 1: GET with no Authorization header → 401 `{error:'unauthorized'}`
    - Test 2: GET with `Authorization: Bearer wrong-secret` → 401
    - Test 3: POST with no Authorization header → 401 (behavior preserved from Phase 3)
    - Test 4: POST with `Authorization: Bearer wrong` → 401
    - Test 5: GET with `Authorization: Bearer ${env.CRON_SECRET}` → NOT 401 (200 or 500 acceptable; we only assert ≠ 401 to avoid mocking the whole Gemini pipeline)
    - Test 6: Both GET and POST are exported functions (`typeof GET === 'function' && typeof POST === 'function'`)
  </behavior>
  <action>
    Create `tests/api/cron/daily-run.test.ts` using vitest. Mock `@/lib/agent/data-loader`, `@/lib/agent/gemini-caller`, `@/lib/agent/executor`, `@/lib/agent/prompt-builder`, `@/lib/ai/client` with minimal stubs so the route module loads but ensurePortfolio/ensureMarketData/callGemini never touch real infra. Use `process.env.CRON_SECRET = 'test-cron-secret-xxxxxxxxxxxxxxxx'` in a `beforeAll` block along with all other env vars required by `lib/env.ts` (DATABASE_URL, GEMINI_API_KEY, SESSION_SECRET (≥32 chars), SITE_PASSWORD, FINNHUB_API_KEY). Import `GET` and `POST` from `@/app/api/cron/daily-run/route`. Construct NextRequest via `new Request('http://localhost/api/cron/daily-run', { method, headers })` cast to NextRequest. Assert status codes.

    RED step: this test MUST fail initially because the current `GET` handler always returns 405 (test 5 expects not-401, not-405 → will fail). Run `pnpm vitest run tests/api/cron/daily-run.test.ts` and confirm RED, then commit: `test(05-01): add failing auth-guard test for daily-run GET+POST`.
  </action>
  <verify>
    <automated>pnpm vitest run tests/api/cron/daily-run.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/api/cron/daily-run.test.ts` exists
    - `grep -c "export" tests/api/cron/daily-run.test.ts` shows test blocks for GET auth, POST auth, and symbol presence
    - vitest output shows tests RED on first run (before Task 2)
  </acceptance_criteria>
  <done>Test file committed (RED); next task will make it GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor daily-run/route.ts — extract handleDailyRun(), add GET</name>
  <files>app/api/cron/daily-run/route.ts</files>
  <read_first>
    - app/api/cron/daily-run/route.ts (全文 — 既存 POST ロジックを破壊せず抽出する)
    - .planning/phases/05-deployment-hardening/05-RESEARCH.md (§Pattern 2, §Pitfall 1, §Code Example 2)
    - tests/api/cron/daily-run.test.ts (直前に書いた RED テスト)
  </read_first>
  <action>
    Refactor `app/api/cron/daily-run/route.ts`:

    1. Keep `import 'server-only'` and all existing imports. Keep `export const maxDuration = 120` (D-12, do NOT change).
    2. Extract the current POST function body into a new private helper:
       ```ts
       async function handleDailyRun(request: NextRequest): Promise<NextResponse> {
         // 1. CRON_SECRET 認証
         const header = request.headers.get('authorization') ?? ''
         const expected = `Bearer ${env.CRON_SECRET}`
         if (header !== expected) {
           return NextResponse.json(
             { error: 'unauthorized', reason: 'bad or missing authorization header' },
             { status: 401 },
           )
         }
         // ... 既存の try { ... } catch { ... } ブロックをそのまま移す
       }
       ```
    3. Replace the existing 405 `GET` handler with:
       ```ts
       // Vercel Cron invokes with GET [CITED: vercel.com/docs/cron-jobs/manage-cron-jobs#securing-cron-jobs]
       export async function GET(request: NextRequest): Promise<NextResponse> {
         return handleDailyRun(request)
       }
       ```
    4. Replace the existing POST with the thin delegator:
       ```ts
       // Manual curl / debug invocation
       export async function POST(request: NextRequest): Promise<NextResponse> {
         return handleDailyRun(request)
       }
       ```
    5. Preserve all existing comments referencing T-03-09/T-03-10/T-03-11 and D-16/D-17. Add comment `// Phase 05 Plan 01 / D-01: GET added for Vercel Cron (Pitfall 1 fix)`.
    6. Do NOT touch any business logic inside the try block — this is a pure refactor from POST body → shared helper.

    After edit, run `pnpm vitest run tests/api/cron/daily-run.test.ts` → MUST turn GREEN. Run `pnpm tsc --noEmit` → MUST pass. Commit: `feat(05-01): add GET handler to daily-run route for Vercel Cron`.
  </action>
  <verify>
    <automated>pnpm vitest run tests/api/cron/daily-run.test.ts --reporter=dot && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export async function GET" app/api/cron/daily-run/route.ts` returns a line that calls `handleDailyRun`
    - `grep -n "export async function POST" app/api/cron/daily-run/route.ts` returns a line that calls `handleDailyRun`
    - `grep -n "async function handleDailyRun" app/api/cron/daily-run/route.ts` returns 1 match
    - `grep -n "maxDuration = 120" app/api/cron/daily-run/route.ts` returns 1 match (D-12 preserved)
    - `grep -n "method_not_allowed" app/api/cron/daily-run/route.ts` returns 0 matches (old 405 GET deleted)
    - vitest file passes (all assertions GREEN)
    - `pnpm tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>GET and POST both delegate to handleDailyRun; Wave-0 unit tests green; type-check clean.</done>
</task>

<task type="auto">
  <name>Task 3: Create vercel.json with crons declaration</name>
  <files>vercel.json</files>
  <read_first>
    - .planning/phases/05-deployment-hardening/05-RESEARCH.md (§Pattern 1, §Code Example 1)
    - .planning/phases/05-deployment-hardening/05-CONTEXT.md (D-01, D-02, D-03)
  </read_first>
  <action>
    Create `vercel.json` at repo root with the following exact content:

    ```json
    {
      "$schema": "https://openapi.vercel.sh/vercel.json",
      "crons": [
        {
          "path": "/api/cron/daily-run",
          "schedule": "0 22 * * *"
        }
      ]
    }
    ```

    Notes (per RESEARCH.md):
    - Schedule is UTC-only (Vercel does not honor timezones). `0 22 * * *` = UTC 22:00 = JST 07:00 (D-02).
    - Hobby plan allows max 1 daily cron and has ±59 min delivery precision — this is acceptable per D-02.
    - Do NOT add `headers`, `rewrites`, or `redirects` keys. Security headers live in `next.config.ts` (Plan 05-02).
    - Do NOT add any env-var definition in vercel.json (D-08: manual Vercel Dashboard UI only).

    Run `pnpm next build` locally → Vercel config schema check happens at deploy time, so a local build is a smoke test only. Commit: `feat(05-01): add vercel.json with daily-run cron schedule`.
  </action>
  <verify>
    <automated>node -e "const c=require('./vercel.json'); if(!Array.isArray(c.crons)||c.crons[0].path!=='/api/cron/daily-run'||c.crons[0].schedule!=='0 22 * * *'){process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - `vercel.json` exists at repo root
    - JSON is valid (`node -e "require('./vercel.json')"` exits 0)
    - `crons[0].path === '/api/cron/daily-run'`
    - `crons[0].schedule === '0 22 * * *'`
    - Top-level keys are exactly `$schema` and `crons` — no `headers`, `rewrites`, `redirects`
    - `pnpm tsc --noEmit && pnpm next build` exits 0 (smoke test)
  </acceptance_criteria>
  <done>vercel.json committed; cron schedule declaration present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vercel Cron platform → /api/cron/daily-run | Platform injects `Authorization: Bearer $CRON_SECRET` header; endpoint must verify against `env.CRON_SECRET` |
| Public internet → /api/cron/daily-run | Anyone can hit the URL; only Bearer holders may invoke the daily cycle |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Spoofing | GET/POST /api/cron/daily-run | mitigate | `handleDailyRun()` compares `request.headers.get('authorization')` to `Bearer ${env.CRON_SECRET}` and returns 401 on mismatch; verified by Task 1 vitest auth-guard tests (GET + POST × missing/wrong) |
| T-05-05 | DoS (405 infinite loop) | GET handler | mitigate | Task 2 replaces hard-coded 405 GET with delegator to `handleDailyRun()`; Vercel Cron (GET-only) will now receive 200 or 401 instead of 405 forever |
| T-05-08 | Tampering | vercel.json schedule | accept | vercel.json is committed to git; any change requires a PR diff review. ±59 min Hobby precision is an accepted platform behavior per D-02 |
| T-05-09 | Repudiation | cron invocation logs | mitigate (existing) | Phase 3 already persists full `DecisionTranscript` + `costUsd` in `decisions` table via `saveDecisionRecord()`; no change needed |
| T-05-10 | DoS (duplicate delivery) | decisions INSERT | mitigate (existing) | Phase 3 D-16 idempotent `ON CONFLICT DO NOTHING` on `(portfolio_id, run_date)` UNIQUE already present; verified in existing handler body (untouched by refactor) |
</threat_model>

<verification>
1. `pnpm vitest run tests/api/cron/daily-run.test.ts` — GREEN (6 assertions)
2. `pnpm tsc --noEmit` — exits 0
3. `pnpm next build` — exits 0 (smoke)
4. `node -e "const c=require('./vercel.json'); console.log(c.crons[0])"` — prints `{ path: '/api/cron/daily-run', schedule: '0 22 * * *' }`
5. Manual curl post-deploy (documented in Plan 05-05 ROLLOUT.md, not this plan):
   - `curl -i https://<domain>/api/cron/daily-run` → 401
   - `curl -i -H "Authorization: Bearer $CRON_SECRET" https://<domain>/api/cron/daily-run` → 200 or 200-skipped
</verification>

<success_criteria>
- Wave 0 unit test file exists and passes
- daily-run/route.ts exports GET and POST, both delegating to a single `handleDailyRun()` helper that enforces the Bearer check
- `maxDuration = 120` preserved (D-12)
- vercel.json at repo root declares the single `/api/cron/daily-run` cron at `0 22 * * *`
- `pnpm tsc --noEmit && pnpm next build` succeed locally
- OPS-01 (cron scheduled) and OPS-02 (Bearer guard) are structurally satisfied; final runtime verification happens in Plan 05-05 ROLLOUT.md post-deploy
</success_criteria>

<output>
After completion, create `.planning/phases/05-deployment-hardening/05-01-SUMMARY.md` listing:
- vercel.json content
- daily-run/route.ts diff summary (extracted handleDailyRun + GET added)
- test file coverage of GET/POST auth guard
- Link to Pitfall 1 in RESEARCH.md
</output>
</content>
</invoke>