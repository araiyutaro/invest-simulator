---
phase: 01-foundation
plan: 05
type: execute
wave: 2
depends_on: [01-foundation/01-02]
files_modified:
  - app/_spikes/gemini/route.ts
  - lib/ai/client.ts
  - .planning/research/AI-LAYER-SPIKE.md
  - .planning/PROJECT.md
  - package.json
autonomous: false
requirements: [SEC-02]
must_haves:
  truths:
    - "Google Gemini API via `@google/generative-ai` Hello World が get_price + place_order function calling で動作する（ローカル）"
    - "Vercel Preview でも同じ route が動き、function call 経由の構造化判断 JSON が返る（D-09）"
    - "SPIKE 結果（実測応答時間・トークン使用量・function call挙動）が AI-LAYER-SPIKE.md に記録される（D-11）"
    - "PROJECT.md Key Decisions の AI Layer 行が `Pending → Confirmed` に更新される（D-10）"
    - "採用されたクライアント初期化が lib/ai/client.ts に server-only 保護で配置される（SEC-02）"
    - "SPIKE フォルダ `app/_spikes/` は Task 3 で完全削除される（D-10 のクリーンアップ）"
  artifacts:
    - path: ".planning/research/AI-LAYER-SPIKE.md"
      provides: "Gemini SPIKE report"
      min_lines: 30
    - path: "lib/ai/client.ts"
      provides: "Production Gemini client (server-only)"
      contains: "import 'server-only'"
  key_links:
    - from: "PROJECT.md Key Decisions"
      to: "AI-LAYER-SPIKE.md"
      via: "reference link"
      pattern: "AI-LAYER-SPIKE"
---

<objective>
D-07 から D-11 に従って AI Layer SPIKE を実施する。`@google/generative-ai` SDK で Hello World エージェント（`get_price` + `place_order` を function declaration で定義）を実装し、ローカル実測 + Vercel Preview デプロイで動作確認し、採用クライアントを `lib/ai/client.ts` に昇格する。

Purpose: Phase 3 (Agent Pipeline) が依存する AI Layer 実装方針を確定する。STATE.md で BLOCKER としてマークされている最大の不確実性を Phase 1 で解消する。

Pivot context: 当初は `@anthropic-ai/sdk` と `@anthropic-ai/claude-agent-sdk` の2者比較 SPIKE を予定していたが、Anthropic 有料 / ユーザーが既に有料 Gemini アカウント保有 のため Gemini 単一 SDK に切り替え。2者比較が不要になったため SPIKE タスク数は 3 → 2（Task 3 は存続：clean up & promote）。

Output: Gemini spike route、AI-LAYER-SPIKE.md レポート、PROJECT.md 更新、SPIKE コードの lib/ai/client.ts への昇格、app/_spikes/ 完全削除
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@.planning/PROJECT.md
@lib/env.ts

<interfaces>
`@google/generative-ai` v0.24 GoogleGenerativeAI + Function Calling:

```typescript
import { GoogleGenerativeAI, SchemaType, FunctionDeclaration } from '@google/generative-ai'
import { env } from '@/lib/env'

const genAI = new GoogleGenerativeAI(env.GEMINI_API_KEY)

const getPriceDeclaration: FunctionDeclaration = {
  name: 'get_price',
  description: 'Get current stock price for a symbol',
  parameters: {
    type: SchemaType.OBJECT,
    properties: {
      symbol: { type: SchemaType.STRING, description: 'Ticker symbol, e.g. AAPL' },
    },
    required: ['symbol'],
  },
}

const placeOrderDeclaration: FunctionDeclaration = {
  name: 'place_order',
  description: 'Place a virtual buy/sell/hold decision with reasoning',
  parameters: {
    type: SchemaType.OBJECT,
    properties: {
      symbol: { type: SchemaType.STRING },
      action: { type: SchemaType.STRING, enum: ['BUY', 'SELL', 'HOLD'] },
      quantity: { type: SchemaType.INTEGER },
      reasoning: { type: SchemaType.STRING },
    },
    required: ['symbol', 'action', 'quantity', 'reasoning'],
  },
}

const model = genAI.getGenerativeModel({
  model: 'gemini-2.0-flash',
  tools: [{ functionDeclarations: [getPriceDeclaration, placeOrderDeclaration] }],
  systemInstruction: 'You are a virtual fund manager managing a JPY 10M portfolio. Use get_price to check the current price, then use place_order to record a BUY/SELL/HOLD decision with your reasoning.',
})

const chat = model.startChat()
const result = await chat.sendMessage('What should we do with AAPL today?')
const call = result.response.functionCalls()?.[0]
// Execute the function locally and send result back...
```

Notes:
- `gemini-2.0-flash` は 2026 時点で Vercel serverless 互換の安定モデル
- Function calling は `functionCalls()` で取得、結果を `functionResponse` 部品として返す
- 無料枠: 15 RPM / 1,500 RPD（gemini-1.5-flash）、2.0 Flash も同等の無料枠
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @google/generative-ai + write Gemini spike route</name>
  <files>package.json, app/_spikes/gemini/route.ts</files>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md D-07, D-08, D-09
    - .planning/phases/01-foundation/01-RESEARCH.md (Gemini pivot addendum)
    - .planning/research/PITFALLS.md #3 (Vercel timeout), #7 (token cost)
    - lib/env.ts (GEMINI_API_KEY 参照方法)
  </read_first>
  <action>
    **Step 1**: Gemini SDK をインストール:
    ```bash
    npm install @google/generative-ai@^0.24.0
    ```

    **Step 2**: `app/_spikes/gemini/route.ts` を作成。GET で `get_price` + `place_order` function calling を使い AAPL の売買判断を Gemini に依頼し、応答時間と function call 情報を返す:

    ```typescript
    import 'server-only'
    import {
      GoogleGenerativeAI,
      SchemaType,
      type FunctionDeclaration,
    } from '@google/generative-ai'
    import { env } from '@/lib/env'

    export const runtime = 'nodejs'
    export const maxDuration = 60

    const genAI = new GoogleGenerativeAI(env.GEMINI_API_KEY)

    const getPriceDeclaration: FunctionDeclaration = {
      name: 'get_price',
      description: 'Get current stock price for a symbol',
      parameters: {
        type: SchemaType.OBJECT,
        properties: {
          symbol: { type: SchemaType.STRING, description: 'Ticker symbol, e.g. AAPL' },
        },
        required: ['symbol'],
      },
    }

    const placeOrderDeclaration: FunctionDeclaration = {
      name: 'place_order',
      description: 'Place a virtual buy/sell/hold decision with reasoning',
      parameters: {
        type: SchemaType.OBJECT,
        properties: {
          symbol: { type: SchemaType.STRING },
          action: { type: SchemaType.STRING, enum: ['BUY', 'SELL', 'HOLD'] },
          quantity: { type: SchemaType.INTEGER },
          reasoning: { type: SchemaType.STRING },
        },
        required: ['symbol', 'action', 'quantity', 'reasoning'],
      },
    }

    function runFakeTool(name: string, args: Record<string, unknown>) {
      if (name === 'get_price') {
        return { symbol: args.symbol, price: 150.0, currency: 'USD', date: '2026-04-11' }
      }
      if (name === 'place_order') {
        return { success: true, recorded: args }
      }
      return { error: 'unknown tool' }
    }

    export async function GET() {
      const startedAt = Date.now()
      try {
        const model = genAI.getGenerativeModel({
          model: 'gemini-2.0-flash',
          tools: [{ functionDeclarations: [getPriceDeclaration, placeOrderDeclaration] }],
          systemInstruction:
            'You are a virtual fund manager managing a JPY 10M portfolio. Use get_price to check the current price, then use place_order to record a BUY/SELL/HOLD decision with your reasoning.',
        })
        const chat = model.startChat()

        // Step 1: ask Gemini
        let result = await chat.sendMessage('What should we do with AAPL today?')
        const trace: Array<Record<string, unknown>> = []
        let safety = 0

        // Step 2: run function calls until the model returns plain text
        while (safety++ < 5) {
          const calls = result.response.functionCalls() ?? []
          if (calls.length === 0) break
          const functionResponses = calls.map((call) => {
            const output = runFakeTool(call.name, call.args ?? {})
            trace.push({ name: call.name, args: call.args, output })
            return {
              functionResponse: {
                name: call.name,
                response: output,
              },
            }
          })
          result = await chat.sendMessage(functionResponses as never)
        }

        return Response.json({
          sdk: 'gemini',
          model: 'gemini-2.0-flash',
          elapsedMs: Date.now() - startedAt,
          usage: result.response.usageMetadata ?? null,
          trace,
          finalText: result.response.text(),
        })
      } catch (error) {
        return Response.json(
          {
            sdk: 'gemini',
            error: error instanceof Error ? error.message : String(error),
            stack: error instanceof Error ? error.stack : undefined,
            elapsedMs: Date.now() - startedAt,
          },
          { status: 500 },
        )
      }
    }
    ```

    注意: この route は Plan 04 の proxy.ts matcher で認証対象になるため、ローカルでは login 後にブラウザからアクセスする必要がある（これで十分、proxy の除外は追加しない）。

    NEVER: SPIKE route を production deploy に残す（Task 3 で削除）。NEVER: `GEMINI_API_KEY` を `NEXT_PUBLIC_` 付きで参照する（D-21）。
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json');const d=p.dependencies;if(!d['@google/generative-ai']){process.exit(1)}" && test -f app/_spikes/gemini/route.ts && head -1 app/_spikes/gemini/route.ts | grep -q "server-only" && grep -q "GoogleGenerativeAI" app/_spikes/gemini/route.ts && grep -q "functionCalls" app/_spikes/gemini/route.ts && grep -q "gemini-2.0-flash" app/_spikes/gemini/route.ts && npx tsc --noEmit --skipLibCheck</automated>
  </verify>
  <acceptance_criteria>
    - package.json dependencies に `@google/generative-ai` が存在
    - `app/_spikes/gemini/route.ts` が存在し、1 行目が `import 'server-only'`
    - `GoogleGenerativeAI`, `functionCalls`, `gemini-2.0-flash` の3つの文字列が source に含まれる
    - route が `elapsedMs`, `usage`, `trace`, `finalText` を返す（または 500 エラー + `error.message`）
    - TypeScript コンパイルエラーなし
  </acceptance_criteria>
  <done>Gemini SPIKE が実装され型チェック OK</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Run Gemini SPIKE locally + on Vercel Preview, write report</name>
  <files>.planning/research/AI-LAYER-SPIKE.md, .planning/PROJECT.md, .planning/STATE.md</files>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md D-08, D-09, D-10, D-11
    - .planning/STATE.md Blockers/Concerns section
  </read_first>
  <what-built>
    Gemini Hello World SPIKE が `app/_spikes/gemini/route.ts` に実装されている。`GEMINI_API_KEY` が .env.local に設定されている。
  </what-built>
  <how-to-verify>
    **Step A — ローカル実測**:
    1. `.env.local` の `GEMINI_API_KEY=` に本物の Gemini API キーが入っていることを確認
    2. `npm run dev` でサーバー起動
    3. ブラウザで `/login` してから、新しいタブで `http://localhost:3000/_spikes/gemini` を開く
    4. JSON response を記録（`elapsedMs`, `usage`, `trace`（function call 順）, `finalText`）
    5. 期待値: `trace` に `get_price` → `place_order` の 2 ステップが記録され、`finalText` に Gemini の最終コメントが入る
    6. エラーなら `error.message` を記録

    **Step B — Vercel Preview 実測 (D-08, D-19 の一時的例外)**:
    1. `git checkout -b spike/gemini-ai-layer`
    2. 現在の状態を commit して push（Vercel が自動で Preview deploy を作成）
    3. Vercel Dashboard → Settings → Environment Variables → Preview 環境に一時的に `GEMINI_API_KEY`, `DATABASE_URL`, `DATABASE_URL_DIRECT`, `SESSION_SECRET`, `SITE_PASSWORD`, `CRON_SECRET` を追加（build を通すため）
    4. Redeploy Preview し、Preview URL で `/login` してから `/_spikes/gemini` を開く
    5. レスポンスを記録
    6. 確認後、Vercel Preview env から 6 変数を **削除**（D-19 に戻す）
    7. `git checkout master && git branch -D spike/gemini-ai-layer`

    **Step C — 判定 (D-09)**:
    1. Vercel Preview で function calling が動き `trace` と `finalText` が返る → 合格、続行
    2. エラー → STOP し原因を調査（モデル名・SDK バージョン・ネットワーク）

    **Step D — Report 作成 (D-11)**: `.planning/research/AI-LAYER-SPIKE.md` を新規作成。見出し:
    - `# AI Layer SPIKE Result (Gemini)` (日付)
    - `## Pivot Context` — Anthropic→Gemini 切り替えの理由
    - `## Test Environment` — ローカル / Vercel Preview、Node.js バージョン、SDK バージョン
    - `## Local Result` — elapsedMs, usage (promptTokenCount, candidatesTokenCount, totalTokenCount), trace, finalText
    - `## Vercel Preview Result` — 同上
    - `## Function Calling Verdict` — 動いた/動かなかった、動作したモデル
    - `## Decision` — 採用モデル（`gemini-2.0-flash` 想定）、理由
    - `## Rollout plan` — Phase 3 Agent Pipeline でどう使うか

    **Step E — PROJECT.md 更新 (D-10)**: Key Decisions テーブルの `AI Layer: Google Gemini API ...` 行を編集:
    - Outcome 列: `— Pending (SPIKE Phase 1)` → `✓ Good — Confirmed 2026-04-11 [.planning/research/AI-LAYER-SPIKE.md]`

    **Step F — STATE.md の Blocker 解消**: もし Blockers/Concerns セクションに「AI Layer未確定」「Vercel タイムアウト矛盾」等があれば削除。

    完了したら "approved"。
  </how-to-verify>
  <action>See <how-to-verify> section above. This is a checkpoint task — human executes the described steps and signals completion via the resume-signal.</action>
  <verify>
    <automated>MANUAL — verification is performed by the human per the how-to-verify steps. Claude proceeds only after 'approved' resume signal.</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/research/AI-LAYER-SPIKE.md` が存在し 30 行以上、全 7 見出しを含む
    - `.planning/PROJECT.md` Key Decisions の AI Layer 行が `Pending → Confirmed` に更新
    - local + Vercel Preview 両方の測定結果がレポートに含まれる
    - 採用モデルが明確に記録される
  </acceptance_criteria>
  <resume-signal>Type "approved" once the report is written and PROJECT.md is updated</resume-signal>
  <done>SPIKE 完了、Gemini 確定、全ドキュメント更新</done>
</task>

<task type="auto">
  <name>Task 3: Promote Gemini client to lib/ai/client.ts, delete spike folder</name>
  <files>lib/ai/client.ts, app/_spikes/</files>
  <read_first>
    - .planning/research/AI-LAYER-SPIKE.md（Task 2 で作成した Decision セクション）
    - .planning/PROJECT.md Key Decisions の更新された AI Layer 行
  </read_first>
  <action>
    **Step 1**: 採用された Gemini クライアントの初期化を `lib/ai/client.ts` に昇格させる:

    ```typescript
    import 'server-only'
    import { GoogleGenerativeAI } from '@google/generative-ai'
    import { env } from '@/lib/env'

    // Shared Gemini client for production use (Phase 3 Agent Pipeline).
    // Decision: Selected @google/generative-ai after Phase 1 SPIKE.
    // See .planning/research/AI-LAYER-SPIKE.md
    export const genAI = new GoogleGenerativeAI(env.GEMINI_API_KEY)

    export const GEMINI_MODEL = 'gemini-2.0-flash'
    ```

    **Step 2**: SPIKE フォルダ全体を削除（D-10 のクリーンアップ）:
    ```bash
    rm -rf app/_spikes
    ```

    NEVER: SPIKE コードを commented out として残す（D-10 違反）。
  </action>
  <verify>
    <automated>test -f lib/ai/client.ts && head -1 lib/ai/client.ts | grep -q "server-only" && grep -q "GoogleGenerativeAI" lib/ai/client.ts && grep -q "GEMINI_MODEL" lib/ai/client.ts && ! test -d app/_spikes && node -e "const p=require('./package.json');const d=p.dependencies;if(!d['@google/generative-ai']){process.exit(1)}" && npx tsc --noEmit --skipLibCheck</automated>
  </verify>
  <acceptance_criteria>
    - `lib/ai/client.ts` が存在し、1行目が `import 'server-only'`
    - `lib/ai/client.ts` に `GoogleGenerativeAI` と `GEMINI_MODEL` export が存在
    - `app/_spikes/` ディレクトリが存在しない
    - `@google/generative-ai` が dependencies に残っている
    - TypeScript 型チェックエラーなし
  </acceptance_criteria>
  <done>Gemini クライアントが lib/ai/client.ts に昇格、SPIKE フォルダ削除、型チェック通過</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process.env.GEMINI_API_KEY → Google Generative AI API | Secret must stay server-side |
| SPIKE routes → production | Must not ship to prod |
| Gemini function response → executor | Untrusted output, validate via Zod before acting (Phase 3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-19 | I (Info Disclosure) | lib/ai/client.ts | mitigate | `import 'server-only'` 1行目。Client Component からの import は build error |
| T-01-20 | I | GEMINI_API_KEY | mitigate | lib/env.ts 経由でのみ参照。`NEXT_PUBLIC_` 使用禁止（D-21） |
| T-01-21 | T (Tampering) | SPIKE routes in prod | mitigate | Task 3 で app/_spikes ディレクトリ完全削除（rm -rf）。prod deploy 前の git に残さない |
| T-01-22 | I | SPIKE result error leak | mitigate | error.message のみ返却、Task 3 で完全削除 |
| T-01-23 | T | Gemini function arguments tampering | accept | Phase 1 はダミーツール。Phase 3 で zod 検証を導入 |
</threat_model>

<verification>
- `test -f lib/ai/client.ts` && `head -1 lib/ai/client.ts` == `import 'server-only'`
- `! test -d app/_spikes`
- package.json に `@google/generative-ai` が残っている
- `.planning/research/AI-LAYER-SPIKE.md` が 30 行以上
- `.planning/PROJECT.md` Key Decisions の AI Layer 行が Confirmed
</verification>

<success_criteria>
Phase 1 Success Criteria #5（「AI Layer選択が実測に基づいて確定し、PROJECT.md Key Decisions に記録されている」）を完全達成。Gemini が Function Calling で動作することを Vercel Preview で実測確認済み。
</success_criteria>

<output>
.planning/phases/01-foundation/01-05-SUMMARY.md に結果を記録
</output>
