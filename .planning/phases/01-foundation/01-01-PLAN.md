---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - db/schema.ts
  - db/index.ts
  - drizzle.config.ts
  - package.json
  - drizzle/migrations/
autonomous: false
requirements: [SEC-03]
must_haves:
  truths:
    - "Neon Postgresに6テーブル全てが存在する（portfolios, positions, trades, decisions, price_snapshots, portfolio_snapshots）"
    - "decisions テーブルは (portfolio_id, run_date) 複合UNIQUE制約を持つ（D-04冪等性）"
    - "全金額カラムが numeric(18,4) 型で定義されている（D-02）"
    - "db/index.ts は server-only でインポート保護されクライアントバンドルに漏れない（SEC-03）"
    - "decisions.transcript は JSONB かつ TypeScript 型付き（DecisionTranscript）である（D-01）"
    - "trades.decision_id が decisions.id への FK として機能する（D-06）"
  artifacts:
    - path: "db/schema.ts"
      provides: "6 Drizzleテーブル定義 + DecisionTranscript型"
      contains: "pgTable('portfolios'|'positions'|'trades'|'decisions'|'price_snapshots'|'portfolio_snapshots'"
    - path: "db/index.ts"
      provides: "drizzle client singleton with server-only guard"
      contains: "import 'server-only'"
    - path: "drizzle.config.ts"
      provides: "drizzle-kit 設定"
      contains: "schema: './db/schema.ts'"
  key_links:
    - from: "trades.decision_id"
      to: "decisions.id"
      via: "references()"
      pattern: "references\\(\\) => decisions"
    - from: "db/index.ts"
      to: "@neondatabase/serverless"
      via: "neon(process.env.DATABASE_URL!)"
      pattern: "neon\\(process\\.env\\.DATABASE_URL"
---

<objective>
Drizzle ORM スキーマで6テーブル全てを定義し、drizzle-kit push で live Neon DB に反映させる。以降全フェーズの永続化層の土台。

Purpose: Phase 2 (Market Data) 以降が型安全に DB に書き込めるようにする。Phase 3 の agent transcript 保存・Phase 4 のダッシュボード読み取り全てこのスキーマに依存する。
Output: db/schema.ts (6テーブル + 型), db/index.ts (drizzle client), drizzle.config.ts, Neon DB にマイグレーション済みテーブル
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@./AGENTS.md
@./package.json

<interfaces>
<!-- 主要な Drizzle API 参照 - RESEARCH.md Pattern 1 より引用 -->

Drizzle ORM (drizzle-orm ^0.45.2) — from RESEARCH.md Code Examples:

```typescript
import {
  pgTable, uuid, numeric, jsonb, text, date,
  timestamp, boolean, integer, unique
} from 'drizzle-orm/pg-core'

// numeric(18,4): { precision: 18, scale: 4 }
// jsonb + TypeScript型: jsonb('col').$type<MyType>().notNull()
// composite unique: (t) => [unique().on(t.col1, t.col2)]
```

Neon HTTP driver (@neondatabase/serverless ^1.0.2) — from RESEARCH.md Pattern 2:

```typescript
import { neon } from '@neondatabase/serverless'
import { drizzle } from 'drizzle-orm/neon-http'

const sql = neon(process.env.DATABASE_URL!)
export const db = drizzle({ client: sql, schema })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Drizzle + Neon dependencies</name>
  <files>package.json</files>
  <read_first>
    - ./package.json (現在 deps は next 16.2.3, react 19.2.4 のみ)
    - .planning/phases/01-foundation/01-RESEARCH.md Standard Stack セクション（version verified 2026-04-11）
  </read_first>
  <action>
    次のコマンドを実行する（D-20 の env 契約、D-21 の server-only 規則を満たすため必要な全 dep を一括で入れる）:

    ```bash
    npm install drizzle-orm@^0.45.2 @neondatabase/serverless@^1.0.2 server-only zod@^3.24
    npm install -D drizzle-kit@^0.31.10
    ```

    package.json の scripts に次を追加:
    ```json
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:push": "drizzle-kit push",
    "db:studio": "drizzle-kit studio"
    ```

    Next.js 16 のビルド互換を念のため npm run build で確認しない（まだ env 未設定なので失敗する）。
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json');const d={...p.dependencies,...p.devDependencies};for(const k of ['drizzle-orm','@neondatabase/serverless','server-only','zod','drizzle-kit']){if(!d[k]){console.error('missing',k);process.exit(1)}}console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - package.json dependencies に drizzle-orm, @neondatabase/serverless, server-only, zod が存在
    - package.json devDependencies に drizzle-kit が存在
    - package.json scripts に db:generate, db:migrate, db:push, db:studio が存在
    - `node_modules/drizzle-orm/package.json` が version 0.45.x を示す
  </acceptance_criteria>
  <done>全依存がインストールされ、package.json に db:* スクリプトが追加されている</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Define Drizzle schema for 6 tables</name>
  <files>db/schema.ts, db/index.ts, drizzle.config.ts</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md Code Examples セクション（Drizzle numeric + JSONB 型付きスキーマ全体像）
    - .planning/phases/01-foundation/01-CONTEXT.md D-01〜D-06
  </read_first>
  <behavior>
    - db/schema.ts は 6テーブル (portfolios, positions, trades, decisions, price_snapshots, portfolio_snapshots) を正確に定義する
    - 全金額カラムが numeric(18,4) (precision:18, scale:4) を使う (D-02)
    - decisions.transcript は jsonb().$type&lt;DecisionTranscript&gt;().notNull() (D-01)
    - decisions に unique().on(portfolioId, runDate) 複合制約 (D-04)
    - trades.decision_id は decisions への FK (D-06)
    - positions に unique().on(portfolioId, symbol) 複合制約
    - price_snapshots に asset_class text not null default 'equity' (D-03 の FX スナップショット対応)
    - price_snapshots に unique().on(symbol, priceDate)
    - portfolio_snapshots に unique().on(portfolioId, snapshotDate)
    - db/index.ts の最初の行は `import 'server-only'` (SEC-03)
    - drizzle.config.ts は dialect:'postgresql', schema:'./db/schema.ts', out:'./drizzle/migrations' を指定
  </behavior>
  <action>
    `db/schema.ts` を作成する。RESEARCH.md の「Drizzle numeric + JSONB 型付きスキーマ全体像」セクションの code example をそのまま採用する（すでに D-01..D-06 に準拠して検証済み）。具体的には:

    1. DecisionTranscript 型を export する（fields: raw_messages, system_prompt, input_data_snapshot, usage — RESEARCH.md Code Examples より）
    2. `portfolios`: id uuid PK defaultRandom, name text notNull, base_currency text notNull default 'JPY', initial_cash numeric(18,4) notNull, cash numeric(18,4) notNull, created_at timestamptz notNull defaultNow
    3. `positions`: id uuid PK, portfolio_id uuid FK → portfolios.id, symbol text, exchange text, quantity integer default 0, avg_cost numeric(18,4), currency text, opened_at/updated_at timestamptz. UNIQUE(portfolio_id, symbol)
    4. `decisions`: id uuid PK, portfolio_id FK, run_date date, summary text, transcript jsonb().$type&lt;DecisionTranscript&gt;().notNull(), token_cost_estimate numeric(18,4), confidence text, model_used text, created_at timestamptz. UNIQUE(portfolio_id, run_date) ← D-04
    5. `trades`: id uuid PK, portfolio_id FK, decision_id uuid FK → decisions.id (D-06), symbol, action text, quantity integer, executed_price numeric(18,4), commission numeric(18,4), currency text, fx_rate_to_jpy numeric(12,6), executed_at timestamptz
    6. `price_snapshots`: id uuid PK, symbol, price_date date, close numeric(18,4), currency text, fx_rate_to_jpy numeric(12,6), market_closed boolean default false, asset_class text notNull default 'equity' ← D-03, source text, fetched_at timestamptz. UNIQUE(symbol, price_date)
    7. `portfolio_snapshots`: id uuid PK, portfolio_id FK, snapshot_date date, total_value_jpy/cash_jpy/positions_value_jpy numeric(18,4), created_at timestamptz. UNIQUE(portfolio_id, snapshot_date)

    `db/index.ts` を作成する:
    ```typescript
    import 'server-only'
    import { neon } from '@neondatabase/serverless'
    import { drizzle } from 'drizzle-orm/neon-http'
    import * as schema from './schema'

    const sql = neon(process.env.DATABASE_URL!)
    export const db = drizzle({ client: sql, schema })
    ```

    `drizzle.config.ts` を作成する:
    ```typescript
    import { defineConfig } from 'drizzle-kit'

    export default defineConfig({
      dialect: 'postgresql',
      schema: './db/schema.ts',
      out: './drizzle/migrations',
      dbCredentials: { url: process.env.DATABASE_URL! },
    })
    ```

    trades と decisions の相互参照で TypeScript の循環参照にならないよう、decisions を trades より先に定義する（`decisionId: uuid('decision_id').references(() => decisions.id)` が forward reference 不可のため）。

    NEVER use: `NEXT_PUBLIC_` prefix anywhere; pg driver; raw prices without server-only guard.
  </action>
  <verify>
    <automated>npx tsc --noEmit db/schema.ts db/index.ts 2>&amp;1 | tee /tmp/tsc.log; grep -c "pgTable" db/schema.ts; grep -c "numeric.*18.*4" db/schema.ts; grep -c "jsonb" db/schema.ts; grep -c "unique().on" db/schema.ts; grep "server-only" db/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pgTable" db/schema.ts` >= 6
    - `grep "precision: 18, scale: 4" db/schema.ts` matches >= 8 occurrences（全金額カラム）
    - `grep "unique().on" db/schema.ts` matches >= 4 (positions, decisions, price_snapshots, portfolio_snapshots)
    - `grep "jsonb('transcript').\$type<DecisionTranscript>().notNull()" db/schema.ts` matches
    - `head -1 db/index.ts` equals `import 'server-only'`
    - `npx tsc --noEmit --skipLibCheck db/schema.ts` に型エラーなし
    - drizzle.config.ts に `dialect: 'postgresql'` と `schema: './db/schema.ts'` が存在
  </acceptance_criteria>
  <done>db/schema.ts / db/index.ts / drizzle.config.ts が上記条件を満たしてコミット可能</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: [BLOCKING] Provision Neon DB + push schema</name>
  <files>.env.local, drizzle/migrations/</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md Pitfall 3（pooled URL を migrate に使わない — direct URL 必須）
    - .planning/phases/01-foundation/01-CONTEXT.md D-17, D-18（開発/本番分離、.env.local のみ）
  </read_first>
  <what-built>
    Drizzle スキーマが `db/schema.ts` に完全定義されている。drizzle.config.ts と db/index.ts も用意済み。
    これから Neon DB をプロビジョニングし、スキーマを live DB に push する。
  </what-built>
  <how-to-verify>
    次の手順を実行してから "approved" で Claude に戻す:

    1. **Neon project 作成**: https://console.neon.tech で新規プロジェクト作成（region: aws-us-east-1 or ap-northeast-1）
    2. **Direct connection URL を取得**: Dashboard → Connection Details → "Direct connection" を選択（`.neon.tech` ドメイン、`.pooler.neon.tech` ではない方）。Pitfall 3 参照
    3. **ローカル .env.local を作成**（まだ存在しない場合）:
       ```
       DATABASE_URL=postgresql://user:pass@ep-xxx.region.neon.tech/neondb
       ```
       （Plan 02 で他の env を追加する。ここでは DATABASE_URL のみで十分）
    4. **スキーマを push**:
       ```bash
       npx drizzle-kit push
       ```
       対話的に "create table" 確認が出たら Yes
    5. **検証**（Neon Console の SQL Editor で）:
       ```sql
       SELECT table_name FROM information_schema.tables
       WHERE table_schema='public' ORDER BY table_name;
       ```
       期待: `decisions, portfolio_snapshots, portfolios, positions, price_snapshots, trades`（6行）
    6. **UNIQUE 制約確認**:
       ```sql
       SELECT conname FROM pg_constraint WHERE contype='u' ORDER BY conname;
       ```
       期待: portfolio_snapshots / positions / decisions / price_snapshots の4つの composite unique が存在

    問題があれば詳細を伝える。なければ "approved"。
  </how-to-verify>
  <action>See <how-to-verify> section above. This is a checkpoint task — human executes the described steps and signals completion via the resume-signal.</action>
  <verify>
    <automated>MANUAL — verification is performed by the human per the how-to-verify steps. Claude proceeds only after 'approved' resume signal.</automated>
  </verify>
  <acceptance_criteria>
    - Neon DB 上に 6 テーブルが作成されている
    - 4つの composite UNIQUE 制約が存在する
    - .env.local に DATABASE_URL が設定され、git ignore 済み
    - `.env.local` が `.gitignore` に含まれている（Next.js デフォルトで含まれている想定、要確認）
  </acceptance_criteria>
  <resume-signal>Type "approved" once 6 tables are created in Neon and DATABASE_URL is in .env.local</resume-signal>
  <done>Live Neon DB にスキーマが反映され、ユーザーが確認済み</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB driver → client bundle | Neon driver とクライアント Supabase-like の誤インポートを防ぐ必要がある |
| process.env → runtime | DATABASE_URL は Route Handler / Server Component からのみ参照されるべき |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | I (Information Disclosure) | db/index.ts | mitigate | `import 'server-only'` を最初の行に記述し、Client Component からのインポートを build 時にエラーにする |
| T-01-02 | T (Tampering) | decisions table | mitigate | (portfolio_id, run_date) UNIQUE 制約 + onConflictDoNothing で同日二重書き込みを防止（Phase 3で利用） |
| T-01-03 | R (Repudiation) | decisions.transcript | mitigate | JSONB で完全な Claude prompt/response を永続化し、後から検証可能に |
| T-01-04 | I | DATABASE_URL | accept | .env.local に平文で格納。ローカル環境のためリスク低。本番は Vercel env vars で分離（D-17） |
</threat_model>

<verification>
- `grep -c "pgTable" db/schema.ts` >= 6
- `head -1 db/index.ts` == `import 'server-only'`
- Neon DB 上に 6 テーブル存在（human-verified in Task 3）
- `npx tsc --noEmit --skipLibCheck` が schema.ts / index.ts で通る
</verification>

<success_criteria>
Phase 1 Success Criteria #3（「Drizzle スキーマが Neon にマイグレーション済みで、全テーブルが存在する」）を完全に達成。
</success_criteria>

<output>
.planning/phases/01-foundation/01-01-SUMMARY.md に結果を記録
</output>
