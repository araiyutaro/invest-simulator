---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.example
  - lib/env.ts
  - .gitignore
autonomous: true
requirements: [SEC-02]
must_haves:
  truths:
    - ".env.example が全必須キー名を列挙し、値は空（D-18）"
    - "lib/env.ts は 5つの必須 env を起動時に zod で検証する"
    - "lib/env.ts は server-only インポートで保護される（SEC-02）"
    - "NEXT_PUBLIC_ プレフィックスが env files に一切存在しない（D-21）"
    - ".env.local が .gitignore に含まれる"
  artifacts:
    - path: ".env.example"
      provides: "env キー名一覧（値なし）"
      contains: "DATABASE_URL="
    - path: "lib/env.ts"
      provides: "runtime env validator + typed env object"
      contains: "import 'server-only'"
  key_links:
    - from: "lib/env.ts"
      to: "process.env"
      via: "zod schema.parse(process.env)"
      pattern: "z\\.object"
---

<objective>
環境変数のコントラクトを確立する。`.env.example` で全キーを可視化し、`lib/env.ts` で起動時に zod 検証を行うことで「envが揃っていれば動く、揃っていなければ早期失敗する」保証を作る。

Purpose: Plan 03 (auth) と Plan 05 (AI SPIKE) が安全に `env.ANTHROPIC_API_KEY`, `env.SESSION_SECRET`, `env.SITE_PASSWORD` を参照できるようにする。`NEXT_PUBLIC_*` 禁止を型レベルで強制する（D-21）。
Output: .env.example, lib/env.ts, .gitignore 確認
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .env.example with all required keys (D-20)</name>
  <files>.env.example, .gitignore</files>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md D-18, D-20, D-21
    - ./.gitignore (存在すれば現在の内容)
  </read_first>
  <action>
    `.env.example` を新規作成し、D-20 で定義された 5 つの必須キーをコメント付きで列挙する。値は空文字。`NEXT_PUBLIC_*` プレフィックスは絶対に使わない（D-21）:

    ```
    # invest-simulator environment variables
    # Copy this file to .env.local and fill in values.
    # NEVER prefix any of these with NEXT_PUBLIC_ (D-21: all secrets are server-only).
    # Do NOT commit .env.local.

    # Neon Postgres direct connection URL (not pooled; pooled URL breaks drizzle-kit migrate).
    # Get from: Neon Console -> Connection Details -> Direct connection
    DATABASE_URL=

    # Anthropic API key for Claude (get from https://console.anthropic.com)
    ANTHROPIC_API_KEY=

    # iron-session cookie encryption secret (MUST be at least 32 bytes)
    # Generate with: openssl rand -base64 48
    SESSION_SECRET=

    # Plaintext dashboard password (D-16: timingSafeEqual compared)
    SITE_PASSWORD=

    # Shared secret for Vercel Cron endpoint protection (Phase 5)
    CRON_SECRET=
    ```

    次に `.gitignore` を確認し、`.env*.local` パターン、`.env.local`、`.env.development.local`、`.env.production.local` のいずれかの形で `.env.local` が ignore されていることを確認。Next.js 16 のデフォルト .gitignore には既に含まれているはずだが、存在しなければ追加する。ただし `.env` や `.env.example` は git 管理対象（ignore しない）。

    NEVER: `NEXT_PUBLIC_DATABASE_URL` のような誤った prefix を使う。
  </action>
  <verify>
    <automated>test -f .env.example &amp;&amp; grep -q "^DATABASE_URL=" .env.example &amp;&amp; grep -q "^ANTHROPIC_API_KEY=" .env.example &amp;&amp; grep -q "^SESSION_SECRET=" .env.example &amp;&amp; grep -q "^SITE_PASSWORD=" .env.example &amp;&amp; grep -q "^CRON_SECRET=" .env.example &amp;&amp; ! grep -q "NEXT_PUBLIC_" .env.example &amp;&amp; grep -E "^\.env\*?\.local" .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - .env.example に DATABASE_URL, ANTHROPIC_API_KEY, SESSION_SECRET, SITE_PASSWORD, CRON_SECRET の5つの空値行がある
    - .env.example に "NEXT_PUBLIC_" 文字列が一切存在しない
    - .gitignore に .env.local パターンがある（`.env*.local` または `.env.local`）
  </acceptance_criteria>
  <done>.env.example がコミット可能な状態、.env.local は gitignore 済み</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Runtime env validator (lib/env.ts)</name>
  <files>lib/env.ts</files>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md D-20, D-21
    - .planning/phases/01-foundation/01-RESEARCH.md（server-only パターン）
  </read_first>
  <behavior>
    - env.ts 起動時に zod schema で process.env を parse する
    - 5つの必須 env (DATABASE_URL, ANTHROPIC_API_KEY, SESSION_SECRET, SITE_PASSWORD, CRON_SECRET) が欠けていれば throw
    - SESSION_SECRET は 32 文字以上を強制（iron-session v8 要件）
    - `import 'server-only'` を最初の行に書く（SEC-02: クライアントバンドルへの誤漏出を build-time error に）
    - export 型: `env: { DATABASE_URL: string; ANTHROPIC_API_KEY: string; SESSION_SECRET: string; SITE_PASSWORD: string; CRON_SECRET: string }`
  </behavior>
  <action>
    `lib/env.ts` を作成:

    ```typescript
    import 'server-only'
    import { z } from 'zod'

    const envSchema = z.object({
      DATABASE_URL: z.string().url().min(1),
      ANTHROPIC_API_KEY: z.string().min(1),
      SESSION_SECRET: z.string().min(32, 'SESSION_SECRET must be at least 32 characters'),
      SITE_PASSWORD: z.string().min(1),
      CRON_SECRET: z.string().min(1),
    })

    const parsed = envSchema.safeParse(process.env)

    if (!parsed.success) {
      const issues = parsed.error.issues
        .map((i) => `  - ${i.path.join('.')}: ${i.message}`)
        .join('\n')
      throw new Error(
        `[env] Missing or invalid environment variables:\n${issues}\n` +
        `See .env.example for the full list.`
      )
    }

    export const env = parsed.data
    export type Env = typeof env
    ```

    これにより、このモジュールを import した瞬間に env がない場合 throw する。Next.js は起動時に各 Route Handler でこれを参照するため、起動に失敗する（fail-fast）。

    NEVER: `process.env.X` を直接 lib/ や app/ 配下で参照しない（drizzle.config.ts と next.config.ts を除く）。常に `import { env } from '@/lib/env'` 経由でアクセスすることを規則化するが、その enforcement は別タスクで行う。
  </action>
  <verify>
    <automated>head -1 lib/env.ts | grep -q "import 'server-only'" &amp;&amp; grep -q "envSchema.safeParse" lib/env.ts &amp;&amp; grep -q "SESSION_SECRET.*min(32" lib/env.ts &amp;&amp; npx tsc --noEmit --skipLibCheck lib/env.ts</automated>
  </verify>
  <acceptance_criteria>
    - `lib/env.ts` の 1行目が `import 'server-only'`
    - zod z.object に 5 つのキー全てが含まれる
    - SESSION_SECRET に `.min(32...)` チェックがある
    - parse 失敗時に Error を throw する
    - `export const env` と `export type Env` の両方がある
    - `npx tsc --noEmit --skipLibCheck lib/env.ts` が通る
  </acceptance_criteria>
  <done>lib/env.ts がコミット可能な状態</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process.env → app code | Secrets must not reach client bundle |
| .env.local → git | Local secrets must never be committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | I (Information Disclosure) | lib/env.ts | mitigate | `import 'server-only'` 1行目強制。Client Component から import すれば build エラー |
| T-01-06 | I | .env.local | mitigate | .gitignore に登録確認、.env.example は値空で template のみ |
| T-01-07 | T (Tampering) | SESSION_SECRET | mitigate | 32 文字未満を zod で reject し、弱いキーでの iron-session 初期化を防ぐ |
| T-01-08 | D (Denial of Service) | app startup | accept | env 欠損で fail-fast。運用上の挙動として受容 |
</threat_model>

<verification>
- `.env.example` に 5 キー全て存在、`NEXT_PUBLIC_` 不在
- `lib/env.ts` が server-only 保護 + zod 検証を実装
- `.env.local` が .gitignore 済み
</verification>

<success_criteria>
Phase 1 Success Criteria #4（「ANTHROPIC_API_KEY・DATABASE_URL・SESSION_SECRET がサーバーサイドのみで参照される」）の土台を提供。
</success_criteria>

<output>
.planning/phases/01-foundation/01-02-SUMMARY.md に結果を記録
</output>
